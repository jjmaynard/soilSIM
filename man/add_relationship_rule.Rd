% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-infilling.R
\name{add_relationship_rule}
\alias{add_relationship_rule}
\title{Add a Relationship Validation Rule}
\usage{
add_relationship_rule(
  config,
  properties,
  relationship_type,
  expected_sum = NULL,
  tolerance = 0.1
)
}
\arguments{
\item{config}{Validation configuration from \code{\link[=create_validation_config]{create_validation_config()}}}

\item{properties}{Character vector of properties involved in the relationship}

\item{relationship_type}{Type of relationship, e.g. \code{"sum"}}

\item{expected_sum}{Expected sum, when \code{relationship_type == "sum"}}

\item{tolerance}{Allowed tolerance around \code{expected_sum}}
}
\value{
Updated validation configuration
}
\description{
Adds a cross-property relationship rule (e.g. a sum-to-100 texture
constraint) to a validation configuration. Note: as in the original
reference implementation, relationship rules are recorded on the config
but are not yet consumed by \code{\link[=apply_validation_rules]{apply_validation_rules()}} (which currently
only enforces \code{range_rules}) - this mirrors the legacy behavior exactly,
not a bug introduced here.
}
