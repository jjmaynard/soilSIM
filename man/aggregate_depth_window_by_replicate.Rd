% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo-simulation.R
\name{aggregate_depth_window_by_replicate}
\alias{aggregate_depth_window_by_replicate}
\title{Thickness-Weighted Mean of Simulated Properties over a Depth Window}
\usage{
aggregate_depth_window_by_replicate(
  sim_long,
  top_depth,
  bottom_depth,
  property_cols,
  replicate_cols = c("mukey", "cokey", "simulation_number")
)
}
\arguments{
\item{sim_long}{Long-format simulated data with \code{hzdept_r}/\code{hzdepb_r} and \code{replicate_cols}.}

\item{top_depth, bottom_depth}{Numeric depth window bounds in cm.}

\item{property_cols}{Character vector of property columns to aggregate.}

\item{replicate_cols}{Columns identifying one replicate (default \code{c("mukey", "cokey", "simulation_number")}).}
}
\value{
One row per replicate, with \code{top}/\code{bottom} set to the requested window and each
property column replaced by its overlap-weighted mean (\code{NA} if the replicate has no overlap
with the window).
}
\description{
For each replicate (a unique combination of \code{replicate_cols}), averages every property column
across that replicate's horizons, weighted by how much of each horizon's thickness overlaps
\verb{[top_depth, bottom_depth]}. Horizons with zero overlap contribute nothing.
}
