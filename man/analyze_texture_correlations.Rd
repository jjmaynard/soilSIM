% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{analyze_texture_correlations}
\alias{analyze_texture_correlations}
\title{Analyze texture (sand/silt/clay) correlations, in both raw and ILR space}
\usage{
analyze_texture_correlations(data, texture_properties, methods, config)
}
\arguments{
\item{data}{Input data with texture \verb{_r} columns.}

\item{texture_properties}{Character vector of available texture column
names (e.g. a subset/all of \code{c("sandtotal_r","silttotal_r","claytotal_r")}).}

\item{methods}{Correlation methods (e.g. \code{c("pearson","spearman")}).}

\item{config}{Unused; kept for interface compatibility with callers.}
}
\value{
\code{list(raw_correlations=, ilr_correlations=, n_observations=, note=)}.
}
\description{
Raw Pearson correlations among compositional (simplex-constrained) parts
like sand/silt/clay percentages are spuriously negative due to the
sum-to-100 constraint - \code{ilr_correlations} (isometric log-ratio space, via
the dependency-free \code{ilr_forward()} in \code{distributions.R}) avoids that
artifact and is the statistically defensible view for compositional data.
Both are reported: \code{raw_correlations} for continuity with prior behavior,
\code{ilr_correlations} as the added analytical value (only computed when all
three fractions are present with at least 5 complete observations).
}
