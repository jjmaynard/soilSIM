% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate-adjustment.R
\name{apply_copula_to_marginals}
\alias{apply_copula_to_marginals}
\title{Map a Joint Copula Sample onto Existing Per-Depth Marginal Distributions}
\usage{
apply_copula_to_marginals(Z, property_matrices, gp_predictions = NULL)
}
\arguments{
\item{Z}{A \code{c(n_depths, k, n_sims)} array as returned by
\code{sample_joint_depth_property_copula()}.}

\item{property_matrices}{Named list of \code{k} matrices (rows = depths, columns = simulations,
\code{dim(Z)[1]}/\code{dim(Z)[3]} must match), in the same property order as \code{Z}'s second dimension -
the same shape \code{preserve_correlation_structure()} already consumes.}

\item{gp_predictions}{Optional named list of per-property depth-trend mean vectors (length
\code{n_depths}), e.g. from \code{predict_gp_depth_trends()}. When supplied for a property, that
property's marginal at each depth is re-centered (a location shift only - shape/spread
preserved) onto the GP-predicted mean before the quantile mapping, replacing the old
sequential \code{gp_ratio} nudge with a single direct shift.}
}
\value{
A named list of \verb{n_depths x n_sims} matrices, same shape/names as \code{property_matrices}.
}
\description{
Converts the standard-normal joint sample from \code{sample_joint_depth_property_copula()} into
actual property values, by probability-integral-transforming each depth/property/realization
cell (\code{pnorm()}) and then inverse-transforming (\code{quantile()}) against that depth/property's
OWN already-simulated marginal distribution (\code{property_matrices[[prop]][depth, ]} - the same
per-horizon triangular-fit values \code{simulate_correlated_triangular()} produced) - a Gaussian
copula, in the standard statistical sense. This is what actually delivers the achieved
correlation structure from \code{sample_joint_depth_property_copula()} onto real property values
while preserving each depth's own fitted marginal shape exactly (for large \code{n_sims}).
}
