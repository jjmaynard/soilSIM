% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{bayesian_updating}
\alias{bayesian_updating}
\title{Bayesian Updating: Conjugate Fusion and General Grid-KDE Fusion}
\description{
Scalar (plain-vector) toolkit for fusing a prior belief
distribution with a likelihood (e.g. an SSURGO-derived prior against
field-measured data).

These are standalone building blocks. The raster fusion pipeline in
\code{core-fusion.R} supplies the equivalent primitives used in the
production SSURGO x SOLUS100 workflow; the tabular Monte Carlo pipeline
in \code{core-montecarlo.R} does not call these functions directly.

Three tiers, in increasing order of generality/cost:
\enumerate{
\item \code{bayes_update_normal_normal()} - exact, closed-form, Normal-only.
\item \code{fuse_beta()}/\code{fuse_gamma()} - exact, closed-form, same-family-only
(each family's kernel parameters simply ADD - the same principle
\code{bayes_update_normal_normal()} uses for Normal's natural parameters,
applied to Beta/Gamma), with a \code{feasible} flag and a documented
moment-based fallback route when infeasible.
\item \code{bayesian_update()} - fully general grid-KDE Bayes' rule (any shape,
any family, even mismatched sides), at the cost of needing raw
samples (not distributional parameters) on both sides.
}

\code{fuse_bivariate_normal()}/\code{fuse_texture_group_from_triplets()} extend
tier 1 to the joint 3-part-composition ILR case (clay/sand/silt texture,
in whichever role order \code{core-montecarlo.R}'s \code{composition_groups$texture$members}
configures - see \code{core-distributions.R}'s ILR section header), since
independent per-fraction fusion (e.g. three separate \code{fuse_beta()}
calls) measurably breaks sum-to-100 (up to about 10 percentage points on
realistic synthetic data).
}
