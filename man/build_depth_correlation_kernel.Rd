% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{build_depth_correlation_kernel}
\alias{build_depth_correlation_kernel}
\title{Build a Depth Correlation Kernel Matrix}
\usage{
build_depth_correlation_kernel(
  depths,
  length_scale,
  kernel = c("exponential", "matern"),
  nu = 1.5,
  boundary_distinctness = NULL,
  distinctness_range = c(min = 1, max = 10),
  min_gate_weight = 0.05
)
}
\arguments{
\item{depths}{Numeric vector of depth values (real units, e.g. cm; need not be evenly spaced or
sorted - internally sorted for the \code{boundary_distinctness} gating pass, then mapped back to
\code{depths}' original order for the returned matrix).}

\item{length_scale}{A single positive numeric length-scale (real units matching \code{depths}),
typically from \code{extract_depth_length_scale()}. A non-finite or non-positive value degrades
gracefully to the identity matrix (no depth correlation) rather than erroring, matching this
package's established fallback pattern for missing/invalid correlation inputs (see
\code{simulate_cokey_generalized()}'s pooled-matrix fallback).}

\item{kernel}{\code{"exponential"} (default) or \code{"matern"}.}

\item{nu}{Matern smoothness parameter (only used when \code{kernel = "matern"}); default \code{1.5}.}

\item{boundary_distinctness}{Optional numeric vector, same length as \code{depths}, of OSD-derived
\code{bound_sd} values (see \code{attach_osd_boundary_distinctness()} -
\code{VERTICAL_CORRELATION_IMPROVEMENT_PLAN.md} Phase 1b/1c) - one value per depth, interpreted as
the distinctness of the boundary immediately ABOVE that depth (in sorted-ascending order; the
value for the shallowest depth is unused, since there is no boundary above the top of the
profile). Smaller \code{bound_sd} (e.g. \code{aqp::hzDistinctnessCodeToOffset()}'s \code{abrupt} ~= 1,
\code{clear} ~= 2.5) means a sharper real discontinuity and more strongly suppresses correlation
across that boundary; larger \code{bound_sd} (\code{gradual} ~= 7.5, \code{diffuse} ~= 10) leaves the plain
distance-decay kernel almost untouched. \code{NULL} (default) or all-\code{NA} skips gating entirely,
reproducing the plain (Phase 1) kernel exactly.}

\item{distinctness_range}{Named \code{c(min=, max=)} giving the \code{bound_sd} values that map to the
strongest (\code{min_gate_weight}) and weakest (\code{1}, no suppression) gating respectively. Default
\code{c(min=1, max=10)} spans \code{aqp::hzDistinctnessCodeToOffset()}'s actual \code{abrupt}-to-\code{diffuse}
range for the codes \code{infill_missing_distinctness()} ever assigns.}

\item{min_gate_weight}{The gating floor (default \code{0.05}) - even the sharpest boundary leaves a
small residual correlation rather than exactly zero, both physically (a truly zero-correlation
entry is rarely justified) and numerically (helps \code{ensure_positive_definite_matrix()}'s
eigenvalue repair stay well-conditioned).}
}
\value{
A symmetric, unit-diagonal, positive-definite \verb{length(depths) x length(depths)}
correlation matrix (repaired via \code{ensure_positive_definite_matrix()} if numerical
floating-point asymmetry from the distance-matrix/gating construction pushes it slightly off).
}
\description{
Constructs an \verb{n_depths x n_depths} correlation matrix from real depth \emph{distances} (not just
adjacent-lag steps, unlike the sequential \code{gp_ratio} nudge this is designed to replace - see
\code{VERTICAL_CORRELATION_IMPROVEMENT_PLAN.md} Phase 1/2), via an exponential or Matern kernel with
a given \code{length_scale}. Intended as the depth half of a Kronecker-separable
\verb{R_depth (x) R_property} joint covariance (Phase 2's \code{sample_joint_depth_property_copula()}).
}
