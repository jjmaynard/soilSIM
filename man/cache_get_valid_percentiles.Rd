% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cache.R
\name{cache_get_valid_percentiles}
\alias{cache_get_valid_percentiles}
\title{Cache-hit lookup for a \code{list(values=, probs=)} percentile structure, with shape validation}
\usage{
cache_get_valid_percentiles(key, ttl_seconds = CACHE_TTL_SECONDS)
}
\arguments{
\item{key, ttl_seconds}{Passed through to \code{cache_get()}.}
}
\value{
The validated, unwrapped \code{list(values=, probs=)}, or \code{NULL} on a miss/stale/malformed
entry.
}
\description{
Combines \code{cache_get()} + \code{unwrap_percentile_list()} + \code{is_valid_percentile_list()} into the one
safe operation every \code{fetch_ssurgo_percentiles()}/\code{fetch_solus_percentiles()} cache-hit call
site needs: a malformed or unreadable hit is treated as a cache miss (returns \code{NULL}, so the
caller re-fetches) rather than being trusted as-is - see \code{is_valid_percentile_list()}'s docs for
why that distinction matters.
}
\keyword{internal}
