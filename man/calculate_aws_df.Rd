% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aws-simulation.R
\name{calculate_aws_df}
\alias{calculate_aws_df}
\title{Calculate Available Water Storage by Depth Interval}
\usage{
calculate_aws_df(sim_data_df)
}
\arguments{
\item{sim_data_df}{A data frame with one row per horizon, with columns
\code{sand_total}, \code{silt_total}, \code{clay_total}, \code{bulk_density_third_bar},
\code{water_retention_third_bar}, \code{water_retention_15_bar} (ROSETTA's
expected input variable names - note these differ from the rest of
\code{soilSIM}'s \code{sandtotal_r}/\code{claytotal_r}/etc. SSURGO-derived naming
convention, since they're ROSETTA's own API contract), plus \code{compname},
\code{hzdept_r}, \code{hzdepb_r}, \code{cokey}.}
}
\value{
A long-format data frame with one row per \code{cokey} per depth slab
actually spanned by that component's horizons (slab boundaries per
\code{slab.structure = c(0, 5, 15, 30, 60, 100)}), with columns \code{cokey},
\code{top}, \code{bottom}, \code{AWHC} (mean available water holding capacity over that
slab). The \code{tidyr::pivot_wider()} step widens on \code{variable} (always
\code{"AWHC"} here, since only one property is slabbed), so it does not
collapse depth slabs into columns - it exists to match \code{aqp::slab()}'s
long output shape to a plain \code{value}-column contract.
}
\description{
Runs \code{soilDB::ROSETTA()} on soil texture/bulk-density/water-retention
inputs to derive van Genuchten pedotransfer parameters, Monte
Carlo-simulates available water holding capacity per horizon via
\code{simulate_vg_aws()}, then depth-slices and summarizes mean AWHC per
component over standard depth intervals via \code{aqp::slab()}.
}
\section{Known limitation}{

This function requires \strong{live network access} - \code{soilDB::ROSETTA()}
POSTs to \verb{https://www.handbook60.org/api/v1/rosetta/<version>} (via
\code{httr::POST()}) rather than computing pedotransfer parameters locally.
Calling this without internet connectivity, or if \code{handbook60.org} is
unreachable, will fail.
}

