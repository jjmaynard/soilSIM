% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{closed_form_percentiles_raster}
\alias{closed_form_percentiles_raster}
\title{Analytic Beta/Gamma percentiles, raster-native, via per-cell \code{terra::app()}}
\usage{
closed_form_percentiles_raster(param1, param2, qfun, posterior_probs)
}
\arguments{
\item{param1, param2}{The two shape-parameter SpatRasters (\code{alpha}/\code{beta} or \code{shape}/\code{rate}).}

\item{qfun}{\code{stats::qbeta} or \code{stats::qgamma} (or any \verb{function(p, param1, param2)}).}

\item{posterior_probs}{Numeric probabilities (0-1).}
}
\value{
Named list of percentile \code{SpatRaster}s.
}
\description{
Unlike the Normal case, \code{qbeta()}/\code{qgamma()}'s quantile is a genuinely nonlinear function of
both the target probability AND the shape parameters, so there is no scalar-arithmetic
shortcut - this dispatches per cell via \code{terra::app()}, but still computes every requested
probability for a cell in one \code{vapply()} call (R's \code{qbeta()}/\code{qgamma()} are already vectorized
over their shape-parameter arguments), not one \code{terra::app()} pass per probability.
}
\keyword{internal}
