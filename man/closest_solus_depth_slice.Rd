% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-solus.R
\name{closest_solus_depth_slice}
\alias{closest_solus_depth_slice}
\title{Snap a Depth Window to the Nearest Native SOLUS Depth Slice}
\usage{
closest_solus_depth_slice(
  top_depth,
  bottom_depth,
  available_slices = c(0, 5, 15, 30, 60, 100, 150)
)
}
\arguments{
\item{top_depth, bottom_depth}{Numeric depth window bounds in cm.}

\item{available_slices}{Native SOLUS depth points to snap to.}
}
\value{
A single numeric value from \code{available_slices}.
}
\description{
\code{soilDB::fetchSOLUS()}'s \code{depth_slices} are single depth points (default \code{c(0, 5, 15, 30, 60, 100, 150)}, live-verified), not ranges, so a \verb{[top_depth, bottom_depth]} window must be
approximated. This function snaps to the native slice closest to the window's midpoint.
}
\section{Relationship to the fusion pipeline}{

\code{fetch_solus_low_pred_high()} uses \code{solus_depth_window_weights()} for a trapezoidal
depth-average across the native slices spanning the window, so the SOLUS likelihood and the
SSURGO prior both represent a thickness-weighted window mean. This function is public API for
callers that want a single nearest slice.
}

\seealso{
\code{solus_depth_window_weights()}
}
