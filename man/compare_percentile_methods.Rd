% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-distributions.R
\name{compare_percentile_methods}
\alias{compare_percentile_methods}
\title{Run several percentile-reconstruction methods on the same data and compare them}
\usage{
compare_percentile_methods(
  quantile_df,
  methods = c("linear_cdf", "spline", "kde"),
  percentile_cols = c("P0", "P5", "P50", "P95", "P100"),
  n = 1000,
  bounds = NULL,
  ...
)
}
\arguments{
\item{quantile_df}{A data frame with at least one row and percentile-named
columns (e.g. "P0", "P5", "P50", "P95", "P100").}

\item{methods}{Character vector of methods to run (see \code{simulate_from_percentiles}).}

\item{percentile_cols}{Character vector of candidate percentile column names.}

\item{n}{Number of samples to draw.}

\item{bounds}{Optional length-2 vector of (lower, upper) bounds. Required
for method = "beta"; used as padding bounds for "spline" if given.}

\item{...}{Additional method-specific arguments (\code{sample_size} for "kde").}
}
\value{
A list with \code{samples} (named list of numeric vectors, one per
method) and \code{summary} (data frame of summary statistics, one row per method).
}
\description{
Draws \code{n} samples from each requested method and returns both the raw
samples and a summary-statistics table (via \code{compute_summary_statistics()})
side by side, so the shape/spread of each reconstruction can be compared
by eye or by downstream tests (e.g. \code{stats::ks.test()} between pairs of methods).
}
\seealso{
Other distributions: 
\code{\link{classify_genhz}()},
\code{\link{ensure_positive_definite_matrix}()},
\code{\link{fit_beta_mle_newton}()},
\code{\link{fit_metalog_linear}()},
\code{\link{fit_normal_triplet}()},
\code{\link{fit_percentile_triplet}()},
\code{\link{ilr_forward}()},
\code{\link{ilr_inverse}()},
\code{\link{quantile_beta}()},
\code{\link{quantile_from_fit}()},
\code{\link{quantile_linear_cdf}()},
\code{\link{quantile_metalog_linear}()},
\code{\link{quantile_triangular}()},
\code{\link{resolve_property_family}()},
\code{\link{validate_correlation_matrix}()}
}
\concept{distributions}
