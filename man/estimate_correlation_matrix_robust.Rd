% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{estimate_correlation_matrix_robust}
\alias{estimate_correlation_matrix_robust}
\title{Estimate a correlation matrix robustly from data, with grouped and global fallbacks}
\usage{
estimate_correlation_matrix_robust(
  data,
  group_var = NULL,
  min_group_n = 5,
  global_fallback = NULL,
  group_fallback_matrices = NULL,
  global_fallback_method = "global_fallback"
)
}
\arguments{
\item{data}{A data frame of numeric property columns (one row per
horizon), optionally with a grouping column named by \code{group_var}.}

\item{group_var}{Optional column name to stratify by (e.g. \code{"genhz"}).}

\item{min_group_n}{Minimum complete-observation count required to trust a
group's (or the pooled data's) empirical correlation over the fallback.}

\item{global_fallback}{Optional fallback correlation matrix; defaults to
the identity matrix over the numeric columns found in \code{data}.}

\item{group_fallback_matrices}{Optional named list of correlation
matrices, keyed by \code{group_var} value, used in place of dropping a group
that fails to meet \code{min_group_n} (today's default behavior when this is
\code{NULL}). A substituted group contributes to the same sample-size-weighted
combination as successfully-estimated groups, with nominal weight
\code{n_obs = min_group_n} (these matrices carry no real sample-size
metadata, so \code{min_group_n} - the same bar local data must already clear
to stand alone - is used as a legible, already-plumbed-through stand-in;
any single successful empirical group already outweighs one substituted
group, and multiple dominate further). A group with no empirical result
AND no matching entry here is still dropped, exactly as before.}

\item{global_fallback_method}{Label for the final-tier \code{method} when
\code{global_fallback} is used because no group (empirical or substituted)
and no pooled fit succeeded. Defaults to \code{"global_fallback"} (today's
exact string); callers supplying a non-identity \code{global_fallback} (e.g.
a KSSL-derived prior) should override this so the returned \code{method}
honestly distinguishes "genuinely uninformative" from "a real borrowed
prior."}
}
\value{
\code{list(matrix=, method=, n_obs=)}. \code{method} is one of
\code{"empirical_grouped"}, \code{"kssl_fallback_grouped"},
\code{"empirical_grouped_kssl_blended"}, \code{"empirical_pooled"}, or
\code{global_fallback_method}'s value (\code{"global_fallback"} by default). The
two \verb{kssl_*}-named values are generic outcomes of any
\code{group_fallback_matrices} use, not KSSL-specific logic in this function
\itemize{
\item named for the fallback source this function was built to support
(\code{kssl-reference-correlations.R}).
}
}
\description{
Ports \code{code_ref/brdf/property_simulation.R}'s \code{simulate_soil_properties()}
correlation pattern: \code{Hmisc::rcorr()} per group when the group has enough
complete observations, else falling back; forces symmetry; repairs
non-positive-definite matrices via \code{Matrix::nearPD()}. When \code{group_var} is
supplied, each qualifying group's empirical matrix is combined into a
single size-weighted average (mod05's simulation architecture uses one
correlation matrix for the whole run, not a per-stratum matrix, so this
folds stratification information in without requiring a larger
architecture change).
}
