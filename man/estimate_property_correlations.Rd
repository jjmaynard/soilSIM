% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte-carlo.R
\name{estimate_property_correlations}
\alias{estimate_property_correlations}
\title{Estimate a Correlation Matrix from Simulation Parameters (Real Implementation)}
\usage{
estimate_property_correlations(
  simulation_params,
  properties,
  config,
  simulation_data = NULL,
  verbose = getOption("ssurgo.verbose", FALSE)
)
}
\arguments{
\item{simulation_params}{List of per-horizon parameter lists.}

\item{properties}{Character vector of property names.}

\item{config}{Simulation configuration.}

\item{simulation_data}{Optional raw per-horizon data frame (same row order
as \code{simulation_params}); its \code{genhz} column, if present, enables
genhz-stratified estimation (an explicit \code{genhz} column always takes
precedence over auto-derivation from \code{hzname}).}

\item{verbose}{Logical; if \code{TRUE}, temporarily raises the package's
log level so \code{INFO}-level progress messages print for the duration
of this call (default \code{FALSE} - quiet). See \code{set_verbose_logging()}.}
}
\value{
\code{list(valid=, correlation_matrix=, info=list(method=, n_obs=))}.
}
\description{
Builds one representative-value-per-horizon data frame - the per-horizon
fitted mean/mode for each property (for composition-group pseudo-properties
like ilr1/ilr2 there's no raw \verb{_r} column, so the fitted mean IS the
representative value) - plus \code{genhz} from \code{simulation_data} if present,
then calls \code{distributions.R}'s \code{estimate_correlation_matrix_robust()}
(\code{Hmisc::rcorr()} + PD-repair, genhz-stratified when possible). Previously
this was a placeholder always returning \code{valid=FALSE}, so
\code{auto_correlation=TRUE} silently fell back to the identity matrix.
}
\details{
When \code{config$monte_carlo$correlation_fallback == "kssl_global"} (opt-in;
defaults to \code{"identity"}, today's exact behavior), a group that fails
empirical estimation falls back to that group's own KSSL reference
correlation matrix (\code{kssl-reference-correlations.R}) instead of being
dropped, and the final identity fallback becomes a KSSL-pooled matrix
instead. \code{genhz} is auto-derived from \code{simulation_data$hzname} via
\code{classify_genhz()} when this is requested and \code{simulation_data$genhz}
isn't already present - scoped narrowly behind the opt-in flag so a
caller who has \code{hzname} but never requests \code{"kssl_global"} sees no
behavior change at all.
}
