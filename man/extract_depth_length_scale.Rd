% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{extract_depth_length_scale}
\alias{extract_depth_length_scale}
\title{Extract a Real-Units Depth Length-Scale from a Fitted GP Model}
\usage{
extract_depth_length_scale(
  gp_model_list,
  depth_scaling = NULL,
  target_corr = exp(-1)
)
}
\arguments{
\item{gp_model_list}{Either the full list returned by \code{fit_local_gp_model_single()}
(\code{list(gp_model, depth_scaling, ...)}) or a raw \code{GPfit}-classed \code{"GP"} object (in which case
\code{depth_scaling} must be supplied separately, or the result stays in scaled \verb{[0,1]} units).}

\item{depth_scaling}{Optional \code{list(min=, max=, range=)} as stored by
\code{fit_local_gp_model_single()}; only needed when \code{gp_model_list} is a raw \code{GP} object without
its own \verb{$depth_scaling}. If neither is available, the returned length-scale stays in scaled
\verb{[0,1]} units.}

\item{target_corr}{The correlation value defining "range" (default \verb{exp(-1) ~= 0.368}).}
}
\value{
A single numeric length-scale in real depth units (or scaled \verb{[0,1]} units if no
\code{depth_scaling} is available), or \code{NA_real_} if \code{gp_model_list} doesn't contain a usable
fitted model (mirrors \code{fit_local_gp_model_single()}'s own \code{NULL}-on-failure contract - callers
should treat \code{NA_real_} the same way they'd treat a \code{NULL} GP model).
}
\description{
\code{GPfit::GP_fit()} fits a power-exponential (or Matern) correlation function
\verb{R(h) = corr(0, h; beta, correlation_param)} over depths already rescaled to \verb{[0,1]} by
\code{fit_local_gp_model_single()}. This reuses that fitted correlation function exactly (via
\code{GPfit::corr_matrix()}, the same internal machinery \code{GP_fit()}/\code{predict.GP()} use) to solve for
the distance \code{h} (in scaled \verb{[0,1]} units) at which the correlation first drops to \code{target_corr}
(default \code{exp(-1)}, the conventional "range" definition for both exponential and Matern
kernels), then rescales \code{h} back to real depth units (cm) using the same \code{depth_scaling}
\code{fit_local_gp_model_single()} stored alongside the model - so no separate/new length-scale
estimation step is introduced; the value is entirely derived from the already-validated GP fit.
}
