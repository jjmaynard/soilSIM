% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-ssurgo-simulate.R
\name{extract_mukey_joint_ensemble}
\alias{extract_mukey_joint_ensemble}
\title{Per-Mukey Joint Multivariate Profile Ensemble at Several Depth Windows}
\usage{
extract_mukey_joint_ensemble(
  aoi_vect,
  depth_windows,
  n_mc = 1000,
  parallel = FALSE,
  n_cores = NULL,
  config = NULL,
  mukey_raster = NULL,
  draws_by_window = NULL,
  properties = SSURGO_SIM_PROPERTY_COLUMNS,
  requested_properties = NULL,
  seed = NULL
)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{depth_windows}{Non-empty list of \code{c(top, bottom)} numeric pairs (cm, \code{bottom > top}).}

\item{n_mc, parallel, n_cores, config}{Passed through to \code{\link{simulate_ssurgo_mapunit_draws}}.}

\item{mukey_raster}{Optional already-fetched mukey raster for this AOI (cache hit otherwise).}

\item{draws_by_window}{Optional pre-computed \code{simulate_ssurgo_mapunit_draws(..., depth_windows =)}
result (named list of per-window data frames) - supply to reuse an existing simulation or to
test offline; when given, \code{aoi_vect}/\code{n_mc}/\code{parallel}/\code{n_cores}/\code{config} are unused.}

\item{properties}{Property columns to retain (default \code{SSURGO_SIM_PROPERTY_COLUMNS}); the
returned \code{properties} element is the subset actually present for this AOI.}

\item{requested_properties}{Passed through to \code{simulate_ssurgo_mapunit_draws()} to restrict the
simulation itself (as opposed to \code{properties}, which only filters columns after the fact).
\code{NULL} (default) restricts to \code{properties}; ignored when \code{draws_by_window} is supplied.}

\item{seed}{Optional integer for opt-in determinism, forwarded to
\code{simulate_ssurgo_mapunit_draws()}; ignored when \code{draws_by_window} is supplied.}
}
\value{
\verb{list(by_mukey = <named list, keyed by mukey code>, properties = <character>, depth_windows = , window_names = , mukey_raster = )}. Each \code{by_mukey} element is
\verb{list(windows = <named list of [n_replicate x n_property] matrices, one per window>, replicate_key = data.frame(cokey, simulation_number))}. \code{NULL} if the simulation fails or no
mukey has a replicate spanning every window.
}
\description{
The multi-property, multi-depth-window analogue of \code{\link{lookup_mukey_draws}} /
\code{\link{lookup_mukey_texture_draws}}: runs the (expensive) SSURGO Monte Carlo once via
\code{\link{simulate_ssurgo_mapunit_draws}} and returns, per mukey, the \strong{retained bag of joint
realizations} - every simulated property together, row-aligned across depth windows, so the
KSSL cross-property and cross-depth rank structure is preserved. This is the input to
\code{remarginalize_ensemble_to_posterior()} (\code{R/core-fusion.R}), which transforms each
pixel's marginals to a fused posterior while keeping this ensemble's empirical copula.
}
\details{
Replicates are aligned across windows on \verb{(cokey, simulation_number)}; a replicate that has no
thickness overlap with some window (so no aggregate there) is dropped from that mukey entirely,
keeping every window's matrix the same number of rows.
}
\seealso{
\code{remarginalize_ensemble_to_posterior()}, \code{\link{lookup_mukey_draws}}
}
\keyword{internal}
