% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-sampling.R
\name{extract_percentile_pairs}
\alias{extract_percentile_pairs}
\title{Parse percentile columns (e.g. "P0","P5","P50") from a one-row data frame}
\usage{
extract_percentile_pairs(quantile_df, percentile_cols)
}
\arguments{
\item{quantile_df}{A data frame with at least one row and percentile-named columns.}

\item{percentile_cols}{Character vector of candidate percentile column names to use.}
}
\value{
A list with \code{probs} (sorted probabilities, 0-1) and \code{values} (matching values).
}
\description{
Shared validation/extraction front end used by every method in
\code{simulate_from_percentiles()}. Column names must be of the form "P\if{html}{\out{<num>}}"
where \if{html}{\out{<num>}} is the percentile (0-100). Columns that are NA or -1 (the
sentinel used elsewhere in this project for "not available") are dropped.
}
\keyword{internal}
