% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{fetch_osd_horizons_cached}
\alias{fetch_osd_horizons_cached}
\title{Fetch OSD horizon distinctness data for a set of soil series names, with per-series caching}
\usage{
fetch_osd_horizons_cached(compnames)
}
\arguments{
\item{compnames}{Character vector of soil series names, as would be passed directly to
\code{soilDB::fetchOSD()}.}
}
\value{
A data frame with columns \code{id}, \code{hzname}, \code{distinctness} - one row per horizon, across
all requested series (any already-cached plus any newly fetched).
}
\description{
\code{soilDB::fetchOSD()} is a live network call. Multiple profiles/components in the same
mukey/AOI frequently share the same series name (e.g. \code{simulate_profile_depths_by_mukey()}
calls \code{query_osd_distinctness()} once per component), so fetching unconditionally on every
call re-requests identical data redundantly. This wrapper caches each series' raw
\code{id}/\code{hzname}/\code{distinctness} rows on disk - the same \code{cache_get()}/\code{cache_set()} mechanism
\code{R/raster-cache.R} already uses for AOI-keyed raster fetches, just with a series-name-keyed
cache key instead - fetching only the series not already cached.
}
\section{Known quirk preserved (not a bug introduced here)}{
 \code{soilDB::fetchOSD()} returns its
\code{id} column upper-cased regardless of the requested case (e.g. requesting \code{"amador"}
returns rows with \code{id == "AMADOR"}) - confirmed via a live call before writing this
function. Splitting a combined multi-series fetch into per-series cache entries is done via
case-insensitive matching on \code{id} to account for this. This casing difference was already
present in \code{soilDB::fetchOSD()}'s output before this caching layer existed, and downstream
code (\code{simulate_and_perturb_soil_profiles()}) was already unaffected by it, since it joins
\code{query_osd_distinctness()}'s output by \code{genhz} only, never by \code{id}.
}

\keyword{internal}
