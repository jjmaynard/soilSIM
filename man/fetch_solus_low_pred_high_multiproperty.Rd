% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-solus.R
\name{fetch_solus_low_pred_high_multiproperty}
\alias{fetch_solus_low_pred_high_multiproperty}
\title{Fetch SOLUS100 Low/Prediction/High Rasters for Multiple Variables, One Depth Window (S1)}
\usage{
fetch_solus_low_pred_high_multiproperty(
  aoi_vect,
  solus_variables,
  top_depth,
  bottom_depth
)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{solus_variables}{Character vector of \code{soilDB::fetchSOLUS()}-recognized variable names.}

\item{top_depth, bottom_depth}{Numeric depth window bounds in cm.}
}
\value{
A named list, one entry per element of \code{solus_variables}, each \code{list(pred=, low=, high=)}
in the same shape \code{fetch_solus_low_pred_high()} returns (a single-layer \code{terra::SpatRaster} or
\code{NULL} per output type). If the single underlying \code{fetchSOLUS()} call itself errors, every
variable's entry is \code{list(pred=NULL, low=NULL, high=NULL)} (a whole-request failure, not a
crash) - callers should fall back to the scalar \code{fetch_solus_low_pred_high()} per variable in
that case. A per-\verb{(variable, output_type)} combo missing from an otherwise-successful response
is \code{NULL} for just that piece, with a \code{warning()} naming it - a partial failure, not a whole-
request one.
}
\description{
Batched sibling of \code{fetch_solus_low_pred_high()}: \code{soilDB::fetchSOLUS()} accepts \code{variables},
\code{depth_slices}, and \code{output_type} all as vectors simultaneously (live-verified 2026-09-04 - see
), so every variable's low/prediction/high
rasters for a window can be fetched in \strong{one} network call instead of \code{3 * length(solus_variables)}
separate ones. The returned layer-naming convention (\code{paste0(variable, "_", depth, "_cm_", suffix)}) was confirmed identical to the single-variable case for the fully-batched request
(\code{nlyr} matched \code{length(variables) * length(depth_slices) * length(output_type)} exactly, no
layers dropped, \code{names()} order not request order but that does not matter since layers are
looked up by exact name).
}
\seealso{
\code{fetch_solus_low_pred_high()}, \code{fetch_solus_percentiles_multiproperty()}
}
