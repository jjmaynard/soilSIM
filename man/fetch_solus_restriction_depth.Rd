% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solus-simulation.R
\name{fetch_solus_restriction_depth}
\alias{fetch_solus_restriction_depth}
\title{Fetch a SOLUS100 Restriction-Depth Raster, Right-Censoring-Guarded (S2)}
\usage{
fetch_solus_restriction_depth(aoi_vect, variable = "anylithicdpt")
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{variable}{\code{"anylithicdpt"} (default, hard-bedrock-specific) or \code{"resdept"} (broader - any
restriction, hard or soft).}
}
\value{
A single-layer \code{terra::SpatRaster} of restriction depth in cm, with every cell
\verb{>= SOLUS_RESTRICTION_CENSOR_CM} recoded to \code{Inf} (no restriction detected - never truncates),
or \code{NULL} if the underlying \code{fetchSOLUS()} prediction fetch fails.
}
\description{
The truncation-depth signal for bedrock/restriction-aware AWC (\code{remarginalized_awc()}'s
\code{restriction_depth} argument, MULTI_PROPERTY_FUSION_PLAN.md task S2). Default variable is
\code{"anylithicdpt"} - trained (per SOLUS100's own published methodology) \strong{only} on
\code{reskind \%in\% c("Lithic bedrock", "Paralithic bedrock")} records, making it hard-bedrock-specific
by construction; no separate SSURGO-side hard/soft classifier is needed since SOLUS already
encodes that distinction across its two site-level variables. \code{"resdept"} is trained on \emph{every}
restriction record regardless of kind (fragipans, duripans, petrocalcic, etc. included) - a
broader, more conservative (more truncation-prone) alternative, offered but not the default,
since it would truncate through partially-penetrable layers with no way to tell those apart from
true bedrock.
}
\seealso{
\code{SOLUS_RESTRICTION_CENSOR_CM}, \code{fetch_solus_site_level()},
\link[=remarginalized_awc]{remarginalized_awc()}'s \code{restriction_depth} argument
}
