% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{fit_beta_mle_newton}
\alias{fit_beta_mle_newton}
\alias{fit_beta_mle_newton_vec}
\title{Vectorized Newton-Raphson Beta MLE from raw-scale percentile values}
\usage{
fit_beta_mle_newton(values, bounds, n_iter = 15, eps = 1e-06)

fit_beta_mle_newton_vec(value_matrix, bounds, n_iter = 15, eps = 1e-06)
}
\arguments{
\item{values}{Numeric vector of \code{k} percentile values (raw units).}

\item{bounds}{\code{c(lower, upper)} physical bounds.}

\item{n_iter}{Fixed Newton-Raphson iteration count.}

\item{eps}{Clamp distance from the exact 0/1 rescaled boundary (avoids
\code{-Inf} in the score equations when a percentile lands exactly on a bound).}

\item{value_matrix}{Numeric matrix, one row per horizon, one column per
percentile value (e.g. 3 columns for an l/r/h triplet).}
}
\value{
\code{list(shape1=, shape2=)}.

A data frame with columns \code{shape1}, \code{shape2} (one row per horizon).
}
\description{
Fits a single \verb{(shape1, shape2)} pair from \code{k} percentile values of one
property at one horizon. The Beta log-likelihood's score/Hessian have a
closed form in \code{digamma()}/\code{trigamma()} (standard Beta MLE theory),
iterated for a fixed number of steps rather than via a general-purpose
optimizer - validated (upstream) against \code{fitdistrplus::fitdist()} to
~1e-3-1e-5 after 15 iterations.

\code{fit_beta_mle_newton_vec()} runs the identical update across
every row of \code{value_matrix} simultaneously (\code{digamma()}/\code{trigamma()} are
already vectorized base-R functions), so it is the version
\code{prepare_simulation_parameters()} should call once per property across
all horizons, rather than looping \code{fit_beta_mle_newton()} per row.
}
