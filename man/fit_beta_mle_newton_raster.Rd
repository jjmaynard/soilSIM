% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution-fitting-raster.R
\name{fit_beta_mle_newton_raster}
\alias{fit_beta_mle_newton_raster}
\alias{quantile_beta_mle_newton_raster}
\title{Fit a Beta distribution's (alpha, beta) rasters via vectorized
Newton-Raphson MLE.}
\usage{
fit_beta_mle_newton_raster(value_rasters, bounds, n_iter = 15, eps = 1e-06)

quantile_beta_mle_newton_raster(fit, q)
}
\arguments{
\item{value_rasters}{List of percentile-value SpatRasters (any count >= 3).}

\item{bounds}{c(lower, upper) physical bounds.}

\item{n_iter}{Fixed Newton-Raphson iteration count (validated sufficient at 15 upstream).}

\item{eps}{Clamp epsilon away from the exact 0/1 boundary.}

\item{fit}{Output of \code{fit_beta_mle_newton_raster()}.}

\item{q}{Target quantile probability.}
}
\value{
list(alpha = SpatRaster, beta = SpatRaster)
}
\description{
If a percentile value lands exactly on \code{bounds} (rescaled x = 0 or 1
exactly), \code{log(x)}/\code{log(1-x)} in the score equations would be \code{-Inf},
degenerating the fit to the clamp floor for every cell uniformly -
unlikely with real empirical percentiles but a real risk for synthetic or
explicitly bound-recorded data. Clamped away from the exact boundary by a
tiny epsilon - a no-op for realistic input.
}
