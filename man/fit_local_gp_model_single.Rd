% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gp.R
\name{fit_local_gp_model_single}
\alias{fit_local_gp_model_single}
\title{Fit a Single Local GP Model}
\usage{
fit_local_gp_model_single(agg_data, prop, gp_control = c(20, 10, 2))
}
\arguments{
\item{agg_data}{One row per depth (\code{hzdept_r}, \code{mean_val}), as returned by
\code{aggregate_property_by_depth()} - always a handful of points (typically <10, one per
unique depth in a single cokey), never one row per Monte Carlo replicate.}

\item{prop}{Property name, stored on the returned model for reference.}

\item{gp_control}{\code{GPfit::GP_fit()}'s \code{control} argument (population size / iteration counts
for its internal hyperparameter search). Defaults to \code{c(20, 10, 2)}, far below \code{GP_fit()}'s
own default \code{c(200*d, 80*d, 2*d)} (\code{d} = input dimensionality, always 1 here - depth is the
only predictor). \code{GP_fit()}'s default search effort scales with \code{d}, not with the number of
training points, and a 1-D, single-hyperparameter (\code{beta}) fit on this few points has a
simple enough likelihood surface that the smaller search converges to the identical
optimum every time - verified empirically across several representative depth/value series
(identical fitted \code{beta} and identical predictions vs. the default, ~5x faster per call).
Pass \code{c(200, 80, 2)} (or larger) to restore \code{GP_fit()}'s own default search effort if a
future property/dataset needs a more thorough search.}
}
\value{
A list with the fitted GP model, depth scaling info, training data, and \code{prop}.
}
\description{
Fit a Single Local GP Model
}
\seealso{
Other gp-modeling: 
\code{\link{adjust_simulation_depthwise}()},
\code{\link{apply_gp_depth_trends}()},
\code{\link{apply_local_gp_adjustments}()},
\code{\link{apply_nrcs_trend_adjustments}()},
\code{\link{convert_to_property_matrices}()},
\code{\link{extract_depth_length_scale}()},
\code{\link{fit_depth_gp_models}()},
\code{\link{fit_individual_gp_model}()},
\code{\link{predict.soilSIM_gp_models}()},
\code{\link{predict_gp_depth_trends}()},
\code{\link{prepare_nrcs_training_data}()},
\code{\link{select_optimal_grouping}()},
\code{\link{validate_gp_models}()}
}
\concept{gp-modeling}
