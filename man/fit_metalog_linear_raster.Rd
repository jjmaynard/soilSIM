% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution-fitting-raster.R
\name{fit_metalog_linear_raster}
\alias{fit_metalog_linear_raster}
\alias{quantile_metalog_linear_raster}
\title{Fit a metalog distribution via linear solve, vectorized as raster arithmetic.}
\usage{
fit_metalog_linear_raster(value_rasters, probs, bounds, boundedness)

quantile_metalog_linear_raster(fit, q, bounds, boundedness)
}
\arguments{
\item{value_rasters}{List of INTERIOR percentile-value rasters (excluding
p=0/p=1 if present).}

\item{probs}{Matching interior probabilities.}

\item{bounds}{\code{c(lower, upper)}; required unless \code{boundedness = "u"}.}

\item{boundedness}{One of \code{"u"}/\code{"sl"}/\code{"su"}/\code{"b"} - see \code{metalog_to_z()}.}

\item{fit}{Output of \code{fit_metalog_linear_raster()}.}

\item{q}{Target quantile probability.}
}
\description{
For a standard percentile setup where the number of interior percentiles
(excluding p=0/p=1, where logit is undefined) equals the number of
metalog terms, the fit is an EXACTLY-DETERMINED linear system
(\code{solve(Y, z)}), not an optimization - same closed-form math as
\code{R/distributions.R}'s \code{fit_metalog_linear()}, vectorized here as raster
arithmetic: \code{Y}/\code{Y_inv} are computed once (fixed, non-spatial probability
grid, via the existing \code{metalog_basis_matrix()}), then applied to
different right-hand-side rasters per coefficient.
}
\details{
CAVEAT (from upstream validation): this is the same fast path \code{rmetalog}
itself takes when its solution is already feasible (implied density
non-negative); \code{rmetalog} also has an LP-based feasibility-correction
fallback for when it isn't - not reproduced here. Use
\code{check_metalog_feasibility_raster()} + \code{quantile_metalog_linear_with_fallback()}
below rather than trusting this fit unconditionally.
}
