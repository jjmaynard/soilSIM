% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{fit_percentile_triplet}
\alias{fit_percentile_triplet}
\title{Fit a distribution to a low/representative/high percentile triplet}
\usage{
fit_percentile_triplet(
  l,
  r,
  h,
  family,
  lh_probs = c(0.05, 0.95),
  bounds = NULL,
  boundedness = if (is.null(bounds)) "u" else "b"
)
}
\arguments{
\item{l, r, h}{Low/representative/high values.}

\item{family}{One of \code{"triangular"}, \code{"uniform"}, \code{"normal"},
\code{"lognormal"}, \code{"beta"}, \code{"metalog"}, \code{"linear_cdf"}, \code{"auto"}.}

\item{lh_probs}{Length-2 vector giving the probabilities \code{l}/\code{h} are
assumed to represent (SSURGO's low/high are professional-judgment
bounds, not confirmed exact percentiles - this makes that assumption
explicit and overridable rather than silently hardcoded).}

\item{bounds}{Optional \code{c(lower, upper)} physical bounds; required for
\code{"beta"} and for \code{"metalog"} when \code{boundedness != "u"}.}

\item{boundedness}{Metalog boundedness; defaults to \code{"u"} when \code{bounds}
is \code{NULL}, else \code{"b"}.}
}
\value{
\code{list(family=, fit=, valid=)}. \code{valid=FALSE} when any of
\code{l}/\code{r}/\code{h} is non-finite.
}
\description{
The single dispatcher \code{extract_property_parameters()} (\code{monte-carlo.R})
calls to turn a SSURGO \verb{_l/_r/_h} triplet into a family-appropriate fit.
}
