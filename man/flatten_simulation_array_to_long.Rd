% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{flatten_simulation_array_to_long}
\alias{flatten_simulation_array_to_long}
\title{Flatten a Monte Carlo Simulation Array to Long Format}
\usage{
flatten_simulation_array_to_long(sim_array, cokey_data)
}
\arguments{
\item{sim_array}{A \verb{[horizon, property, realization]} array as produced by
\code{\link[=simulate_correlated_properties]{simulate_correlated_properties()}}.}

\item{cokey_data}{The original per-horizon data frame passed in as
\code{soil_data} - must have exactly one row per \code{sim_array} horizon index,
in the same order.}
}
\value{
Long-format data frame.
}
\description{
Converts the \verb{[horizon, property, realization]} array returned by
\code{\link[=generate_monte_carlo_realizations]{generate_monte_carlo_realizations()}} (as \code{result$simulation_data}) into a
long-format data frame with one row per horizon-realization combination,
matching the shape \code{\link[=apply_local_gp_adjustments]{apply_local_gp_adjustments()}}, \code{\link[=apply_nrcs_trend_adjustments]{apply_nrcs_trend_adjustments()}},
and \code{\link[=convert_to_property_matrices]{convert_to_property_matrices()}} expect (a \code{simulation_number} column
plus one column per property, alongside the original horizon metadata).
}
