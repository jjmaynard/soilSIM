% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{fuse_adaptive}
\alias{fuse_adaptive}
\title{Fuse a prior and likelihood belief distribution, choosing the fusion
route by AOI size rather than requiring the caller to pick.}
\usage{
fuse_adaptive(
  prior_value_rasters,
  lik_value_rasters,
  percentile_probs,
  family = c("normal", "beta", "gamma"),
  bounds = NULL,
  threshold_cells = 80000,
  n_samples = 500,
  grid_resolution = NULL,
  verbose = TRUE,
  mukey_raster = NULL,
  mukey_draws = NULL,
  posterior_probs = NULL
)
}
\arguments{
\item{prior_value_rasters, lik_value_rasters}{Named lists of percentile-value
\code{SpatRaster}s (e.g. \code{list(P0=, P5=, P50=, P95=, P100=)}), same layer
count/order on both sides.}

\item{percentile_probs}{Numeric probabilities matching the value-raster
list order (e.g. \code{c(0, 0.05, 0.5, 0.95, 1)}).}

\item{family}{One of "normal", "beta", "gamma" - the family both sides
are fit in for the closed-form route, and the family the general
route's output is moment-matched back into for a consistent output
contract across routes.}

\item{bounds}{Required for \code{family = "beta"} (\code{c(lower, upper)}).}

\item{threshold_cells}{AOI cell count at or below which the general
\code{update_prior()} route is used; above it, the closed-form route.}

\item{n_samples}{Samples drawn per side for the general route's KDE fusion.}

\item{grid_resolution}{Passed to \code{update_prior()} for the general route. \code{NULL} (default)
resolves to \link{FUSE_GENERAL_KDE_DEFAULT_GRID_RESOLUTION} (\code{0.1}) - see that constant's docs for
the profiling/accuracy justification for why this differs from \code{update_prior()}'s own
standalone default of \code{0.01}.}

\item{verbose}{If TRUE (default), print the chosen route and why.}

\item{mukey_raster, mukey_draws}{Optional - opts into \code{prior_fusion_method = "raw_draws"} on
whichever route runs (\code{fuse_general_kde()} for the general route -;
\code{fuse_closed_form()} for the closed-form route -, added at negligible cost since
that route's raw_draws fit is a per-mukey precompute + \code{terra::subst()} broadcast, not a
per-cell computation - see \code{mukey_draws_closed_form_fit_raster()}'s docs). \code{NULL} (default)
preserves original behavior exactly on both routes.}

\item{posterior_probs}{Passed through to whichever route runs (\code{fuse_general_kde()} for the
general route, \code{fuse_closed_form()} for the closed-form route - see \link{FUSE_POSTERIOR_DEFAULT_PROBS}).
\code{NULL} (default) resolves to the same rich default on both routes. The general route computes
percentiles from \code{update_prior()}'s discretized posterior (exact relative to
\code{grid_resolution}, but tail percentiles are limited by how well the KDE approximates the tails
from a finite sample); the closed-form route computes them analytically (\code{qnorm}/\code{qbeta}/
\code{qgamma} at the fused family parameters - exact regardless of how extreme the probability).}
}
\value{
A list:
\itemize{
\item \code{posterior}: family-native parameter list (\code{mu}/\code{sigma} for "normal",
\code{alpha}/\code{beta} for "beta", \code{shape}/\code{rate} for "gamma") - always this
shape regardless of which route ran. Also carries \code{percentiles}: a named list of
\code{SpatRaster}s (\code{"P01"}..\code{"P99"} by default), one per \code{posterior_probs} entry, on both
routes.
\item \code{route}: one of \code{"bayesian_update_general"}, \code{"closed_form_normal"},
\code{"closed_form_beta"}, \code{"closed_form_gamma"}.
\item \code{route_detail}: for \code{family \%in\% c("beta","gamma")} on the
closed-form route, a \code{SpatRaster} of 1 (native same-family fusion) /
0 (per-cell fallback). \code{NULL} for "normal" and for the general route.
\item \code{n_fallback_cells}: count of cells that used the fallback.
\item \code{diagnostics}: \code{list(ncell=, threshold_cells=, family=, elapsed_sec=)}.
}
}
\description{
Fuse a prior and likelihood belief distribution, choosing the fusion
route by AOI size rather than requiring the caller to pick.
}
\seealso{
Other raster-fusion: 
\code{\link{align_percentile_probs}()},
\code{\link{build_cache_key}()},
\code{\link{cache_get}()},
\code{\link{cache_set}()},
\code{\link{check_metalog_feasibility_raster}()},
\code{\link{fit_beta_mle_newton_raster}()},
\code{\link{fit_beta_mom_raster}()},
\code{\link{fit_gamma_mom_raster}()},
\code{\link{fit_metalog_linear_raster}()},
\code{\link{fit_normal_raster}()},
\code{\link{percentiles_from_draws}()},
\code{\link{quantile_metalog_linear_with_fallback}()},
\code{\link{remarginalize_awc}()},
\code{\link{remarginalize_ensemble_to_posterior}()},
\code{\link{resolve_property_dist}()},
\code{\link{wrap_nested_rasters}()}
}
\concept{raster-fusion}
