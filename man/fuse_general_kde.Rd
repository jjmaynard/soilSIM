% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{fuse_general_kde}
\alias{fuse_general_kde}
\title{Fully general fusion path for \code{fuse_adaptive()}: per cell, draw samples
from both sides' percentiles (via \code{simulate_from_percentiles(method= "linear_cdf")}), fuse via \code{bayesian_update()}, and moment-match the
posterior samples back into the requested family so the output contract
matches the closed-form route.}
\usage{
fuse_general_kde(
  prior_value_rasters,
  lik_value_rasters,
  percentile_probs,
  family,
  bounds,
  n_samples,
  grid_resolution
)
}
\arguments{
\item{prior_value_rasters, lik_value_rasters}{Lists of percentile-value SpatRasters.}

\item{percentile_probs}{Numeric probabilities matching the value-raster list order.}

\item{family}{One of "normal", "beta", "gamma".}

\item{bounds}{Required for \code{family = "beta"}.}

\item{n_samples}{Samples drawn per side, per cell.}

\item{grid_resolution}{Passed to \code{bayesian_update()}. \code{NULL} (the caller-facing default from
\code{fuse_adaptive()}) resolves to \link{FUSE_GENERAL_KDE_DEFAULT_GRID_RESOLUTION}, not
\code{bayesian_update()}'s own standalone default of \code{0.01} - see that constant's docs for why.}
}
\description{
Fully general fusion path for \code{fuse_adaptive()}: per cell, draw samples
from both sides' percentiles (via \code{simulate_from_percentiles(method= "linear_cdf")}), fuse via \code{bayesian_update()}, and moment-match the
posterior samples back into the requested family so the output contract
matches the closed-form route.
}
\keyword{internal}
