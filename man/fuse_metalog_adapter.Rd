% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{fuse_metalog_adapter}
\alias{fuse_metalog_adapter}
\title{Metalog adapter for \code{fuse_property_adaptive()}: closed-form route via metalog-fitted
Normal-equivalent moments, used regardless of AOI size.}
\usage{
fuse_metalog_adapter(
  prior_value_rasters,
  prior_probs,
  lik_value_rasters,
  lik_probs,
  property_config,
  verbose = TRUE,
  posterior_probs = NULL,
  ...
)
}
\arguments{
\item{posterior_probs}{Numeric probabilities (0-1) at which to report posterior percentiles.
\code{NULL} (default) resolves to \link{FUSE_POSTERIOR_DEFAULT_PROBS}. Computed via
\code{qnorm_percentiles_raster()} on the final fused \verb{(mu, sigma)} - this route already reduces
both sides to Normal-equivalent moments (\code{metalog_moments_raster()}'s quadrature) before the
final \code{bayes_update_normal_normal()} step, so there is no richer distributional shape to draw
percentiles from beyond that Normal approximation; carries forward this route's existing
"less-validated glue code" caveat (see \code{metalog_moments_raster()}'s own docs) to its
percentile output too, not a new limitation introduced here.}

\item{...}{Absorbed and ignored - lets this function sit behind \code{fuse_property_adaptive()}'s
\code{...} passthrough (which also forwards \code{n_samples}/\code{grid_resolution}/\code{mukey_raster}/
\code{mukey_draws} meant for other routes) without erroring on arguments this route doesn't use.}
}
\description{
Metalog adapter for \code{fuse_property_adaptive()}: closed-form route via metalog-fitted
Normal-equivalent moments, used regardless of AOI size.
}
\keyword{internal}
