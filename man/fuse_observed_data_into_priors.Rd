% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte-carlo.R
\name{fuse_observed_data_into_priors}
\alias{fuse_observed_data_into_priors}
\title{Fuse Observed Field/Lab Data into SSURGO-Derived Priors}
\usage{
fuse_observed_data_into_priors(
  simulation_params,
  simulation_data,
  sim_properties,
  properties,
  observed_data,
  composition_plan,
  config,
  verbose = getOption("ssurgo.verbose", FALSE)
)
}
\arguments{
\item{simulation_params}{Per-horizon parameter list from
\code{prepare_simulation_parameters()}.}

\item{simulation_data}{The filtered per-horizon data frame (same row
order as \code{simulation_params}).}

\item{sim_properties}{Character vector \code{simulation_params} is keyed over
(may include composition-group pseudo-properties).}

\item{properties}{The caller-facing, original property vector. Unused
directly (fusion is keyed by \code{sim_properties}/\code{observed_data} names);
kept for interface symmetry with sibling pipeline functions.}

\item{observed_data}{Named list keyed by property name (see shapes above).}

\item{composition_plan}{Result of \code{resolve_composition_groups()}.}

\item{config}{Simulation configuration.}

\item{verbose}{Logical; if \code{TRUE}, temporarily raises the package's
log level so \code{INFO}-level progress messages print for the duration
of this call (default \code{FALSE} - quiet). See \code{set_verbose_logging()}.}
}
\value{
\code{simulation_params}, with fused entries replaced in place.
}
\description{
Orchestrates Bayesian updating against each property's per-horizon prior
fit produced by \code{prepare_simulation_parameters()}, using
\code{bayesian-updating.R}'s pure fusion primitives (\code{bayes_fuse()},
\code{fuse_property()}, \code{fuse_texture_group_from_triplets()}). For every
property present in \code{observed_data}, every horizon's existing prior is
replaced by its fused posterior - each horizon keeps its OWN prior
(so posteriors still vary per horizon), but all incorporate the SAME
shared observed-data likelihood, since field/lab measurements aren't
typically tied to individual simulated horizons at that granularity.
}
\details{
Two supported shapes per non-texture entry in \code{observed_data}:
\itemize{
\item a numeric vector of raw samples -> the fully general route
(\code{fuse_property()}'s \code{bayesian_update()} grid-KDE path). The prior is
first SAMPLED (via \code{quantile_from_fit()}, since that route needs both
sides to already be raw vectors - it fits no parametric family to
either side), and the resulting posterior sample is stored as an
exact empirical quantile function (\code{family = "linear_cdf"}) rather
than forced back into a possibly ill-fitting parametric family.
\item a family-native parameter list (\code{list(mean=, sd=)} for a normal or
lognormal prior/likelihood; \code{list(shape1=, shape2=)} or
\code{list(mean=, var=)} for a beta prior/likelihood) -> the closed-form
route (\code{bayes_fuse()}), only supported for prior families
\code{"normal"}/\code{"lognormal"}/\code{"beta"} (the only families with a conjugate
route). Any other prior family (\code{"triangular"}/\code{"uniform"}/
\code{"metalog"}/\code{"linear_cdf"}) SKIPS fusion for that (horizon, property)
with a logged WARN, continuing to simulate from the unfused prior
rather than erroring the whole pipeline.
}

The texture composition group (if active) is handled jointly: supply all
three of \code{claytotal}/\code{sandtotal}/\code{silttotal} in \code{observed_data} (each
either a \code{c(low=,rep=,high=)} triplet, an unnamed length-3
\code{c(low,rep,high)} vector, or a raw sample vector reduced to one via its
own empirical quantiles at \code{config$monte_carlo$lh_percentile}) - partial
texture entries (1-2 of 3) skip texture fusion entirely with a WARN,
matching the same "some but not all composition members present"
fallback \code{resolve_composition_groups()} already uses. Fusion runs once
per horizon via \code{fuse_texture_group_from_triplets()} (that horizon's own
clay/sand/silt l/r/h as the prior side, the shared observed triplet as
the likelihood side), replacing
\code{simulation_params[[i]][["ilr1"]]}/\verb{[["ilr2"]]}.

Note: this only narrows each property's MARGINAL prior spread. It does
NOT change the cross-property correlation/dependency structure used by
the Cholesky copula in \code{simulate_correlated_properties()} -
\code{estimate_property_correlations()} (Step 5, when \code{auto_correlation=TRUE})
estimates that from \code{simulation_data}'s raw representative values, which
fusion does not touch.
}
