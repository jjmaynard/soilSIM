% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{fuse_property}
\alias{fuse_property}
\alias{fuse_property.default}
\alias{fuse_property.percentile_rasters}
\title{Fuse a prior and likelihood belief distribution, dispatching by input shape}
\usage{
fuse_property(prior, likelihood, ...)

\method{fuse_property}{default}(
  prior,
  likelihood,
  family = NULL,
  bounds = NULL,
  method = NULL,
  n_samples = 1000,
  grid_resolution = 0.01,
  ...
)

\method{fuse_property}{percentile_rasters}(
  prior,
  likelihood,
  prior_probs,
  lik_probs,
  property_config,
  threshold_cells = 80000,
  ...
)
}
\arguments{
\item{prior, likelihood}{Either numeric vectors of raw samples (both sides
must be vectors), or named lists of family-native parameters matching
\code{family} (both sides must be lists).}

\item{...}{Passed to the dispatched method.}

\item{family}{Required when \code{prior}/\code{likelihood} are parameter lists; one
of \code{"normal"}, \code{"beta"}, \code{"gamma"}. Ignored (and inferred as "general")
when \code{prior}/\code{likelihood} are raw-sample vectors.}

\item{bounds}{Unused currently; kept for interface symmetry with
percentile-triplet-based callers.}

\item{method}{Optional assertion/override: \code{"general"} or \code{"closed_form"}.
Errors if it doesn't match what the input shape implies, rather than
silently overriding it.}

\item{n_samples, grid_resolution}{Passed to \code{update_prior()} for the
general route.}

\item{prior_probs, lik_probs}{Percentile levels for \code{prior}/\code{likelihood} (raster method only).}

\item{property_config}{A property configuration list selecting the fusion distribution family
(raster method only) - see \code{\link[=fuse_property_adaptive]{fuse_property_adaptive()}}.}

\item{threshold_cells}{Raster method only; see \code{\link[=fuse_property_adaptive]{fuse_property_adaptive()}}.}
}
\value{
For the general route: a numeric vector of posterior samples
(\code{update_prior()}'s native output). For the closed-form route: the
family-native posterior parameter list (\code{fuse_distribution()}'s native output).
These are NOT the same shape - documented deliberately rather than
forced into a fake-uniform contract, since the caller already knows
which shape it's passing in and therefore which shape it gets back.
}
\description{
Unlike the raster-native reference (\code{fuse_adaptive()}), which dispatches
between the general and closed-form routes by AOI cell count - a concept
with no tabular analogue - this dispatches on the SHAPE of \code{prior}/
\code{likelihood}: atomic numeric vectors (raw samples) route to the fully
general \code{update_prior()}; named lists of family-native parameters
route to the closed-form \code{fuse_distribution()}. This removes the "which
heuristic" ambiguity entirely, since the caller's input shape already
determines which route applies.
}
\seealso{
Other bayesian-fusion: 
\code{\link{convert_beta_to_moments}()},
\code{\link{convert_gamma_to_moments}()},
\code{\link{convert_lognormal_to_normal}()},
\code{\link{convert_moments_to_beta}()},
\code{\link{convert_moments_to_gamma}()},
\code{\link{convert_normal_to_lognormal}()},
\code{\link{fuse_beta}()},
\code{\link{fuse_bivariate_normal}()},
\code{\link{fuse_distribution}()},
\code{\link{fuse_gamma}()},
\code{\link{fuse_normal_normal}()},
\code{\link{update_prior}()}
}
\concept{bayesian-fusion}
