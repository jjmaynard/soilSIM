% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{fuse_property_adaptive}
\alias{fuse_property_adaptive}
\title{Fuse one property's prior and likelihood, dispatching to the right
family/route automatically from \code{property_config}.}
\usage{
fuse_property_adaptive(
  prior_value_rasters,
  prior_probs,
  lik_value_rasters,
  lik_probs,
  property_config,
  threshold_cells = 80000,
  ...
)
}
\arguments{
\item{prior_value_rasters, lik_value_rasters}{Named lists of percentile-value SpatRasters.}

\item{prior_probs, lik_probs}{Matching probability vectors (reconciled via
\code{align_percentile_probs()} if they differ).}

\item{property_config}{List with \code{dist} (one of "normal"/"beta"/"gamma"/
"lognormal"/"metalog"/"auto"), and optionally \code{bounds}/\code{boundedness}/
\code{auto_skew_threshold}.}

\item{threshold_cells}{AOI cell count at or below which \code{fuse_adaptive()}
uses its general route.}

\item{...}{Additional arguments passed to \code{fuse_adaptive()}/\code{fuse_lognormal_adaptive()}/
\code{fuse_metalog_adapter()} (e.g. \code{n_samples}, \code{grid_resolution}, \code{verbose}, \code{posterior_probs} -
see \link{FUSE_POSTERIOR_DEFAULT_PROBS}; reaches every route as of P3.5 - \code{fuse_metalog_adapter()}
absorbs and ignores the arguments it doesn't use, e.g. \code{n_samples}). Also how
\code{mukey_raster}/\code{mukey_draws} (opting into \code{prior_fusion_method = "raw_draws"}, see
\code{fuse_general_kde()}'s docs) reach the general-KDE route for \code{dist \%in\% c("normal","beta","gamma")} - only that route currently supports it (not
\code{fuse_lognormal_adaptive()}'s general branch or \code{fuse_metalog_adapter()}; see
\code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md}).}
}
\value{
\code{list(posterior=, route=, route_detail=, n_fallback_cells=, dist=, dist_source=, skew_proxy=)}.
}
\description{
The raster-native counterpart of \code{bayesian-updating.R}'s \code{fuse_property()},
and this toolkit's top-level entry point for non-compositional properties
(see \code{fuse_texture_group()} for the compositional/texture case) - the
raster analogue of \code{run_stage1_fusion()} minus the SSURGO/SOLUS
fetch-and-cache wrapper (see this file's \verb{@section Deliberately out of scope:} above).
}
