% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{fuse_texture_group}
\alias{fuse_texture_group}
\title{Fuse a compositional group's members JOINTLY via ILR fusion
(\code{R/core-distributions.R}'s \code{estimate_ilr_moments_mc()}/\code{ilr_inverse()},
\code{R/core-fusion.R}'s \code{fuse_bivariate_normal()}), rather than
independently via \code{fuse_beta()} per member - independent fusion
measurably breaks sum-to-100 (up to 10.5 percentage points on realistic
synthetic data). The raster counterpart of the
already-ported scalar \code{fuse_texture_group_from_triplets()}.}
\usage{
fuse_texture_group(
  fetched,
  mukey_raster = NULL,
  mukey_texture_draws = NULL,
  posterior_probs = NULL,
  posterior_n_mc = 2000
)
}
\arguments{
\item{fetched}{A list (one entry per group member, in \code{group_members()}'s
order) of \verb{list(id=, prior=<named list of ALIGNED percentile-value rasters>, prior_probs=, lik=<value rasters>, lik_probs=)}.}

\item{mukey_raster, mukey_texture_draws}{Optional - opts into \code{prior_fusion_method = "raw_draws"} (see \code{fuse_texture_group_batch_core()}'s docs). \code{mukey_raster} must already be
aligned to the same grid as \code{fetched}'s percentile rasters (nearest-neighbor resampled - it's
categorical). \code{mukey_texture_draws} is a \code{lookup_mukey_texture_draws()} result. \code{NULL}
(default) for either uses the percentile-reconstruction path.}

\item{posterior_probs}{Numeric probabilities (0-1) at which to report each fraction's posterior
percentiles. \code{NULL} (default) resolves to \link{FUSE_POSTERIOR_DEFAULT_PROBS}. Computed via
\code{texture_group_percentiles_raster()} - see that function's \verb{@section Sum-to-100 caveat} for
why, unlike \code{value}, these do NOT generally sum to 100 across the group's three members.}

\item{posterior_n_mc}{Monte Carlo draw count per cell for the percentile sampler (default 2000).
See \code{texture_group_percentiles_raster()}'s docs for why this is a separate lever from the
moment-estimation MC sample size used elsewhere in this function.}
}
\value{
A named list keyed by each member's \code{id}:
\verb{list(posterior = list(value = <inverse-ILR point-estimate raster for that fraction>, ilr_mu = <2-layer raster, shared across the group>, ilr_Sigma = <3-layer raster (S11,S12,S22), shared>, percentiles = <named list of percentile SpatRasters for THIS member's own fraction - see the sum-to-100 caveat above>), dist = "texture_ilr", route = "closed_form_ilr_group", route_detail = NULL, n_fallback_cells = 0)}. To draw posterior samples for a specific
fraction/cell directly, extract that cell's \code{ilr_mu}/\code{ilr_Sigma} and pass to
\code{sample_ilr_posterior()}.
}
\description{
Fuse a compositional group's members JOINTLY via ILR fusion
(\code{R/core-distributions.R}'s \code{estimate_ilr_moments_mc()}/\code{ilr_inverse()},
\code{R/core-fusion.R}'s \code{fuse_bivariate_normal()}), rather than
independently via \code{fuse_beta()} per member - independent fusion
measurably breaks sum-to-100 (up to 10.5 percentage points on realistic
synthetic data). The raster counterpart of the
already-ported scalar \code{fuse_texture_group_from_triplets()}.
}
