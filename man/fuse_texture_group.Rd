% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{fuse_texture_group}
\alias{fuse_texture_group}
\title{Fuse a compositional group's members JOINTLY via ILR fusion
(\code{R/distributions.R}'s \code{estimate_ilr_moments_mc()}/\code{ilr_inverse()},
\code{R/bayesian-updating.R}'s \code{fuse_bivariate_normal()}), rather than
independently via \code{fuse_beta()} per member - independent fusion
measurably breaks sum-to-100 (up to 10.5 percentage points on realistic
synthetic data, per upstream validation). The raster counterpart of the
already-ported scalar \code{fuse_texture_group_from_triplets()}.}
\usage{
fuse_texture_group(fetched)
}
\arguments{
\item{fetched}{A list (one entry per group member, in \code{group_members()}'s
order) of \verb{list(id=, prior=<named list of ALIGNED percentile-value rasters>, prior_probs=, lik=<value rasters>, lik_probs=)}.}
}
\value{
A named list keyed by each member's \code{id}:
\verb{list(posterior = list(value = <inverse-ILR point-estimate raster for that fraction>, ilr_mu = <2-layer raster, shared across the group>, ilr_Sigma = <3-layer raster (S11,S12,S22), shared>), dist = "texture_ilr", route = "closed_form_ilr_group", route_detail = NULL, n_fallback_cells = 0)}. To draw posterior samples for a specific
fraction/cell, extract that cell's \code{ilr_mu}/\code{ilr_Sigma} and pass to
\code{sample_ilr_posterior()}.
}
\description{
Fuse a compositional group's members JOINTLY via ILR fusion
(\code{R/distributions.R}'s \code{estimate_ilr_moments_mc()}/\code{ilr_inverse()},
\code{R/bayesian-updating.R}'s \code{fuse_bivariate_normal()}), rather than
independently via \code{fuse_beta()} per member - independent fusion
measurably breaks sum-to-100 (up to 10.5 percentage points on realistic
synthetic data, per upstream validation). The raster counterpart of the
already-ported scalar \code{fuse_texture_group_from_triplets()}.
}
