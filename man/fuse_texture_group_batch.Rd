% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{fuse_texture_group_batch}
\alias{fuse_texture_group_batch}
\alias{fuse_texture_group_batch_core}
\title{Vectorized per-chunk core of \code{fuse_texture_group()}}
\usage{
fuse_texture_group_batch(
  row_mat,
  clay_id,
  sand_id,
  silt_id,
  prior_z,
  lik_z,
  n_mc = 2000,
  max_cells_per_subchunk = 2000,
  mukey_prior_texture_samples = NULL
)

fuse_texture_group_batch_core(
  row_mat,
  clay_id,
  sand_id,
  silt_id,
  prior_z,
  lik_z,
  n_mc = 2000,
  mukey_prior_texture_samples = NULL
)
}
\arguments{
\item{row_mat}{A matrix with one row per cell and the 18
\verb{<id>_<prior|lik>_<lo|p50|hi>} columns \code{fuse_texture_group()} builds (plus one trailing
\code{mukey_code} column when \code{mukey_prior_texture_samples} is supplied).}

\item{clay_id, sand_id, silt_id}{The three members' \code{id}s, in ILR order.}

\item{prior_z, lik_z}{Standard-normal quantiles for the prior/likelihood
low-high intervals (as in \code{estimate_ilr_moments_mc()}'s \code{z} argument).}

\item{n_mc}{Monte Carlo sample size per side (matches
\code{estimate_ilr_moments_mc()}'s default).}

\item{max_cells_per_subchunk}{Upper bound on cells processed by one
\code{fuse_texture_group_batch_core()} call - each cell needs \code{6 * n_mc}
doubles of intermediate MC-draw storage (\code{n_mc=2000} -> ~96KB/cell), so
\code{terra::app()}'s own chunk size (which only accounts for the raw,
non-MC-expanded raster layers) can hand \code{fuse_texture_group()} chunks
large enough to exhaust memory - confirmed empirically: a 50,000-cell
chunk failed with "cannot allocate vector of size 4.5 Gb". Sub-chunking
here, in cell order, doesn't change the RNG stream order (still
cell-major) so results are unaffected.}

\item{mukey_prior_texture_samples}{Optional - opts into \code{prior_fusion_method = "raw_draws"}
(see \code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md} task P2.4). A named list, keyed by mukey (as
character), each element an \verb{n_mc x 3} matrix (columns \code{clay_total}/\code{sand_total}/
\code{silt_total}) of real joint texture draws for that mukey - see
\code{mukey_texture_draws_lookup()}. When supplied, \code{row_mat}'s last column must be the per-cell
mukey code. \code{NULL} (default) preserves original behavior exactly.}
}
\value{
A \verb{nrow(row_mat) x 5} matrix, columns \verb{mu1, mu2, S11, S12, S22}.
}
\description{
Computes, for every cell (row) in \code{row_mat} at once, the same
prior/likelihood ILR moments + bivariate-normal fusion that
\code{estimate_ilr_moments_mc()} + \code{fuse_bivariate_normal()} compute one cell at
a time - see \code{fuse_texture_group()}'s PERF comment at its call site.
}
\details{
RNG draws are generated as a single \code{rnorm()} call ordered so the
underlying standard-normal stream is consumed in exactly the same
cell-major, then prior-before-lik, then clay/sand/silt, then
draw-1..n_mc order the original per-cell loop consumed it in (rnorm()'s
\code{mean}/\code{sd} args only affine-transform each already-drawn standard normal;
they don't change how many values are drawn or in what order) - so this is
a pure vectorization, not a behavior change. \code{fuse_bivariate_normal()}'s
2x2 \code{solve()}-based fusion is replaced with the closed-form 2x2 matrix
inverse formula (\code{solve()} isn't vectorizable across cells), which can
differ from \code{solve()} by floating-point rounding (~1e-10 relative) but is
otherwise the identical linear algebra.
}
\keyword{internal}
