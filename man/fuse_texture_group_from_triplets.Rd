% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian-updating.R
\name{fuse_texture_group_from_triplets}
\alias{fuse_texture_group_from_triplets}
\title{Fuse two full clay/sand/silt low-rep-high triplets jointly via ILR fusion}
\usage{
fuse_texture_group_from_triplets(
  prior_triplets,
  lik_triplets,
  z_prior,
  z_lik,
  n_mc = 2000,
  n_samples = 1000,
  total = 100
)
}
\arguments{
\item{prior_triplets, lik_triplets}{Named list with elements \code{clay},
\code{sand}, \code{silt}, each a length-3 numeric vector \code{c(low, rep, high)}. These
keys are positional-role placeholders matching \code{estimate_ilr_moments_mc()}'s
\code{low_clay}/\code{low_sand}/\code{low_silt} parameter naming (position 1/2/3 of the
ILR sequential binary partition - see \code{distributions.R}'s ILR section
header), not an identity requirement; callers (e.g.
\code{monte-carlo.R}'s \code{fuse_observed_data_into_priors()}) build them from
\code{composition_groups$texture$members} in configured order.}

\item{z_prior, z_lik}{Standard-normal quantile matching each side's
low/high interval (e.g. \code{qnorm(0.95)} for a 5th/95th-percentile
low/high).}

\item{n_mc}{Monte Carlo sample size passed to \code{estimate_ilr_moments_mc()}.}

\item{n_samples}{Number of posterior composition samples to draw.}

\item{total}{Composition target sum (default 100).}
}
\value{
\verb{list(posterior_samples = <n_samples x 3 matrix, columns clay/sand/silt, guaranteed sum-to-}total\verb{ by construction>, ilr_mu=, ilr_Sigma=)}.
}
\description{
Wraps \code{distributions.R}'s \code{estimate_ilr_moments_mc()} (once per side) +
\code{fuse_bivariate_normal()} + \code{sample_ilr_posterior()} for future joint
texture-prior updating. Independent per-fraction fusion (e.g. three
separate \code{fuse_beta()} calls) measurably breaks sum-to-100 (documented
upstream: up to 10.5 percentage points on realistic synthetic data) -
this is why the fusion happens jointly, in ILR space, instead.
}
