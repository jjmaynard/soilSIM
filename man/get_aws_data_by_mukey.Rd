% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{get_aws_data_by_mukey}
\alias{get_aws_data_by_mukey}
\title{Query SSURGO Database for Soil Data by Mukey}
\usage{
get_aws_data_by_mukey(mukeys)
}
\arguments{
\item{mukeys}{A vector of string or numeric map unit keys (mukeys) representing the map units to retrieve data for.}
}
\value{
A data frame with soil horizon data for the given mukeys, including aggregated rock fragment data.
}
\description{
Queries the NRCS SSURGO database (\code{mapunit}/\code{component}/\code{chorizon}/\code{chfrags})
for horizon morphology, texture, bulk density, water-retention, and
rock-fragment-volume data for the given map unit key(s).
}
\details{
This deliberately does not reuse \code{execute_ssurgo_query_working()} (which
also joins \code{mapunit}/\code{component}/\code{chorizon}/\code{chfrags}): that function only
ever selects \code{chf.fragsize_r}, never \code{chf.fragvol_l/r/h}, so its rock
fragment aggregation path (\code{aggregate_rock_fragment_volume_working()})
currently produces no data. This function's entire purpose is summing
\code{fragvol_l/r/h} per horizon, so delegating would silently drop rock
fragment volume - a real regression, not a style difference.
}
