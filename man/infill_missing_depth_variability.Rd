% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{infill_missing_depth_variability}
\alias{infill_missing_depth_variability}
\title{Infill Missing Depth Variability}
\usage{
infill_missing_depth_variability(horizon_data)
}
\arguments{
\item{horizon_data}{A data frame containing horizon depth data. It must include the following columns:
\itemize{
\item \code{hzdept_r}: Representative top depth.
\item \code{hzdept_l}: Top low value (may be missing).
\item \code{hzdept_h}: Top high value (may be missing).
\item \code{hzdepb_r}: Representative bottom depth.
\item \code{hzdepb_l}: Bottom low value (may be missing).
\item \code{hzdepb_h}: Bottom high value (may be missing).
}}
}
\value{
A data frame with missing depth variability values filled in. Missing top low values are replaced with
\code{hzdept_r - 2} and missing top high values with \code{hzdept_r + 2}. Similarly, missing bottom low values are replaced
with \code{hzdepb_r - 2} and missing bottom high values with \code{hzdepb_r + 2}. All values are ensured to be non-negative.
}
\description{
Infills missing depth variability values in a horizon data frame by
replacing missing top and bottom depth bounds with the representative
depth value plus or minus 2, ensuring that the resulting values are not
negative.
}
\examples{
data <- data.frame(
  hzdept_r = c(10, 20),
  hzdept_l = c(NA, 18),
  hzdept_h = c(NA, NA),
  hzdepb_r = c(30, 40),
  hzdepb_l = c(NA, NA),
  hzdepb_h = c(NA, 42)
)
infill_missing_depth_variability(data)
}
