% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion-bridge.R
\name{invert_posterior_cdf_raster}
\alias{invert_posterior_cdf_raster}
\title{Invert a per-pixel posterior CDF at a per-mukey probability, vectorized over the grid}
\usage{
invert_posterior_cdf_raster(pct, post_probs, u_ras)
}
\arguments{
\item{pct}{Named list of percentile-value \code{SpatRaster}s (ascending).}

\item{post_probs}{Matching ascending probabilities.}

\item{u_ras}{A \code{SpatRaster} of probabilities in (0, 1). \strong{May have many layers} - each of the
single-layer \code{pct} rasters is recycled across them, so one call inverts a whole realization
stack in a fixed \code{~4 * (length(post_probs) - 1)} terra ops regardless of \code{nlyr(u_ras)}.}
}
\value{
A \code{SpatRaster} of interpolated posterior values, \code{nlyr(u_ras)} layers.
}
\description{
For a scalar-per-mukey probability raster \code{u_ras}, returns the raster whose every cell is that
cell's posterior value at its \code{u}, by piecewise-linear interpolation on the posterior's
percentile knots with \code{rule = 2} constant extrapolation - the inverse-direction analogue of
\code{sim_linear_cdf_batch()}.
}
\keyword{internal}
