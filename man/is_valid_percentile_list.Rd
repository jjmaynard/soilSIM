% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cache.R
\name{is_valid_percentile_list}
\alias{is_valid_percentile_list}
\title{Validate a \code{list(values=, probs=)} percentile structure}
\usage{
is_valid_percentile_list(x)
}
\arguments{
\item{x}{A candidate value read from \code{cache_get()}, already \code{unwrap_percentile_list()}-ed.}
}
\value{
\code{TRUE} if \code{x} has the expected \verb{list(values = <non-empty list of SpatRaster>, probs = <numeric vector, same length as values, finite, in [0,1]>)} shape, else \code{FALSE}.
}
\description{
\code{cache_get()} returns whatever a cache hit's \code{.rds} deserializes to without checking its shape,
so a stale or corrupted cache entry (e.g. left behind by an interrupted run, or written by a
since-changed code path) would otherwise be trusted as-is by every \code{cache_get()} caller
downstream. A malformed hit with an empty/missing \code{probs} is especially dangerous here: it
doesn't error where it's read, it silently propagates into \code{align_percentile_probs()}'s
\code{range()} calls (degrading into "no non-missing arguments to min/max" warnings) and then into
\code{fuse_property_adaptive()}'s \code{terra::ncell(prior_value_rasters[[1]])} as a cryptic "subscript
out of bounds" error, far from the actual cause. This validator lets cache-hit call sites treat
a malformed hit as a plain cache miss (re-fetch) instead.
}
\keyword{internal}
