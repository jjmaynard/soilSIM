% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{k_fold_gp_cv}
\alias{k_fold_gp_cv}
\title{K-Fold Cross-Validation for GP Correlation Structure Selection}
\usage{
k_fold_gp_cv(X, Y, n_folds, corr_candidates)
}
\arguments{
\item{X}{Predictor matrix (one row per observation).}

\item{Y}{Response vector.}

\item{n_folds}{Number of cross-validation folds.}

\item{corr_candidates}{Named list of \code{corr} specs to pass to
\code{\link[GPfit:GP_fit]{GPfit::GP_fit()}} (e.g. \code{list(type = "exponential", power = 1.95)}).}
}
\value{
List with \code{mean_rmse_by_candidate}, a named list of mean held-out
RMSE per candidate (\code{NA} for a candidate that failed on every fold).
}
\description{
Runs \code{n_folds}-fold cross-validation of \code{\link[GPfit:GP_fit]{GPfit::GP_fit()}} for each supplied
correlation-family candidate, returning the mean held-out RMSE per
candidate. Shared helper used by \code{\link[=optimize_gp_hyperparameters]{optimize_gp_hyperparameters()}} and
\code{perform_gp_cross_validation()} (validation-diagnostics.R) so the
fold-splitting logic is not duplicated across files.
}
