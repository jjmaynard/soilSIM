% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-ssurgo-simulate.R
\name{lookup_mukey_texture_draws}
\alias{lookup_mukey_texture_draws}
\title{Raw Per-Mukey JOINT Texture Draws, Keyed by Mukey (clay/sand/silt Row-Aligned)}
\usage{
lookup_mukey_texture_draws(draws)
}
\arguments{
\item{draws}{A data frame from \code{\link{simulate_ssurgo_mapunit_draws}}, for the same
AOI/depth window the caller's mukey raster covers.}
}
\value{
A named list keyed by mukey (as character), each element a matrix with columns
\code{clay_total}/\code{sand_total}/\code{silt_total} (one row per cokey/replicate with a complete texture
triplet for that row - rows with any missing fraction, e.g. a cokey where texture simulation
failed for that replicate, are dropped rather than kept partially). \code{NULL} if \code{draws} doesn't
carry all three texture columns at all (e.g. no member of the AOI's data had texture data).
}
\description{
The texture-group counterpart of \code{\link{lookup_mukey_draws}}: instead of one property's
values, returns the row-aligned \verb{(clay_total, sand_total, silt_total)} TRIPLES simulated
together for the same cokey/replicate. This preserves the real cross-property correlation
between the three fractions (from \code{simulate_cokey_generalized()}'s KSSL texture correlation
matrix) - something \code{fuse_texture_group()}'s current percentile-reconstruction fusion discards
entirely, since it draws each fraction independently from its own marginal
percentile-derived normal. Used by \code{fuse_texture_group_batch_core()}'s \code{prior_fusion_method = "raw_draws"} opt-in.
}
\keyword{internal}
