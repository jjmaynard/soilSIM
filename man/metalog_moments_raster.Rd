% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{metalog_moments_raster}
\alias{metalog_moments_raster}
\title{Compute a metalog fit's mean/sd via numerical quadrature over its own raw
quantile function (\verb{mean = integral of Q(p) dp}, \verb{var = integral of Q(p)^2 dp - mean^2}).}
\usage{
metalog_moments_raster(
  fit,
  infeasible_r,
  full_value_rasters,
  full_probs,
  bounds,
  boundedness,
  p_grid = seq(0.001, 0.999, by = 0.005)
)
}
\arguments{
\item{fit, infeasible_r, bounds, boundedness}{Same as
\code{check_metalog_feasibility_raster()}'s arguments/output for this \code{fit}.}

\item{full_value_rasters, full_probs}{The FULL percentile set (including
p=0/p=1 if available) - used only for the infeasible-cell Normal fallback.}

\item{p_grid}{Probability grid for the quadrature.}
}
\description{
Compute a metalog fit's mean/sd via numerical quadrature over its own raw
quantile function (\verb{mean = integral of Q(p) dp}, \verb{var = integral of Q(p)^2 dp - mean^2}).
}
\section{Known limitation}{

This is new glue code with no prior validated version (unlike the rest of
this file's math, which was validated upstream against \code{fitdistrplus}/
closed-form references) - spot-check against real data before trusting it
in production, per the original source bundle's own caveat.

Uses \code{quantile_metalog_linear_raster()} (valid for any \code{p} in \verb{(0,1)})
rather than the \verb{_with_fallback()} wrapper for the quadrature itself:
\code{terra::ifel()} isn't lazy, so if the fallback's
\code{quantile_linear_cdf_raster()} were evaluated at every quadrature grid
point, it would error whenever the grid extends outside \code{full_probs}'s
own range. Instead, the quadrature integrates the raw metalog quantile
function over the full range, and INFEASIBLE cells fall back to
\code{fit_normal_raster()}'s closed-form moments computed directly from the
same percentile triplet.
}

