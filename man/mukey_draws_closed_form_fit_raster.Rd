% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{mukey_draws_closed_form_fit_raster}
\alias{mukey_draws_closed_form_fit_raster}
\title{Per-unique-mukey closed-form family fit from real Monte Carlo draws, broadcast to a raster}
\usage{
mukey_draws_closed_form_fit_raster(
  mukey_raster,
  mukey_draws,
  family,
  bounds = NULL
)
}
\arguments{
\item{mukey_raster}{Categorical mukey SpatRaster, aligned to the target grid.}

\item{mukey_draws}{A \code{mukey_draws_lookup()} result - named list keyed by mukey (character), each
element a numeric vector of real simulated draws for that mukey.}

\item{family}{One of "normal", "beta", "gamma", "lognormal". \code{"lognormal"} (added
) fits \code{mu}/\code{sigma} directly from
\code{log(draws)} - the same LOG-SPACE parameterization \code{fuse_lognormal_adaptive()}'s large-AOI
branch already uses internally (\code{normal_to_lognormal_params()}'s output shape), so the merge
there is a drop-in replacement of the percentile-triplet-derived log-space fit, not a new
shape. Non-positive draws are dropped before taking \code{log()} (a real lognormal draw is always
positive; any non-positive value reflects upstream noise, not signal) - a mukey left with fewer
than 2 positive draws falls back to \code{NA} like every other family here.}

\item{bounds}{Required for \code{family = "beta"} - draws are scaled to \verb{[0,1]} before fitting, to
match \code{fit_beta_mle_newton_raster()}'s own scaled-space convention (see that function's docs) -
\code{qfun}/downstream percentile code already expects \code{alpha}/\code{beta} in that scaled space.}
}
\value{
A named list of \code{SpatRaster}s (\code{mu}/\code{sigma}, \code{alpha}/\code{beta}, or \code{shape}/\code{rate}) - \code{NA}
at any cell whose mukey has no \code{mukey_draws} entry. No fallback is applied here; the caller
merges this against the percentile-based fit for such cells (matching every other raw_draws
branch's per-cell degrade-to-percentile-reconstruction convention).
}
\description{
Precomputes a family-native parameter fit (Normal \code{mu}/\code{sigma}, Beta \code{alpha}/\code{beta}, or Gamma
\code{shape}/\code{rate}) once per UNIQUE mukey directly from that mukey's real simulated draws, then
broadcasts the result onto every cell sharing that mukey via one vectorized categorical lookup
(\code{terra::subst()}) - not a per-cell computation, unlike \code{fuse_general_kde()}'s raw_draws branch
(which needs a per-cell \code{density()} call and so is meaningfully slower). Verified cheap
: at 100,172 synthetic cells and 150
unique mukeys, the whole precompute+broadcast pipeline measured ~0.18s total - negligible next
to the existing family-specific fit costs it complements (beta's Newton-Raphson fit alone
measured ~20s at the same cell count), because cost here scales with mukey CARDINALITY, not
cell count.
}
\keyword{internal}
