% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{mukey_draws_percentiles_raster}
\alias{mukey_draws_percentiles_raster}
\title{Per-unique-mukey empirical percentiles from real Monte Carlo draws, broadcast to a raster}
\usage{
mukey_draws_percentiles_raster(mukey_raster, mukey_draws, probs)
}
\arguments{
\item{mukey_raster}{Categorical mukey SpatRaster, aligned to the target grid.}

\item{mukey_draws}{A \code{lookup_mukey_draws()} result - named list keyed by mukey (character), each
element a numeric vector of real simulated draws for that mukey.}

\item{probs}{Numeric probabilities (0-1) at which to compute each mukey's empirical quantile -
pass the same knot probabilities the caller's \code{fit_metalog_linear_raster()} call will use.}
}
\value{
A named list of \code{SpatRaster}s, one per \code{probs} entry (named \code{paste0("P", round(probs * 100))}, matching this file's other percentile-list conventions) - \code{NA} at any cell whose mukey
has no \code{mukey_draws} entry. No fallback is applied here; the caller merges this against the
percentile-reconstruction values for such cells, same convention as every other raw_draws branch.
}
\description{
Companion to \code{mukey_draws_closed_form_fit_raster()} for routes that consume percentile VALUES
directly rather than a family parameter fit - \code{fuse_metalog_adapter()}, whose metalog fit
is an exact linear interpolation through fixed percentile knots (\code{solve()} on a basis matrix), not
a moment/density fit. There is no "metalog fit to raw draws" analogous to
\code{mukey_draws_closed_form_fit_raster()}'s normal/beta/gamma/lognormal cases - the real extension
for a percentile-interpolation method is to interpolate through REAL empirical percentiles
(\code{stats::quantile()} on the actual draws) instead of the percentile-reconstruction values. Same
per-unique-mukey precompute + \code{terra::subst()} broadcast pattern as its sibling - cheap, scales
with mukey cardinality, not cell count.
}
\keyword{internal}
