% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte-carlo.R
\name{normalize_monte_carlo_config}
\alias{normalize_monte_carlo_config}
\title{Normalize a Monte Carlo \code{simulation_config} to be Properly Nested}
\usage{
normalize_monte_carlo_config(
  simulation_config,
  default_config,
  verbose = getOption("ssurgo.verbose", FALSE)
)
}
\arguments{
\item{simulation_config}{User-supplied config (flat or nested).}

\item{default_config}{Result of \code{get_monte_carlo_defaults()}, used only to
recognize which flat key names belong under \code{monte_carlo}.}

\item{verbose}{Logical; if \code{TRUE}, temporarily raises the package's
log level so \code{INFO}-level progress messages print for the duration
of this call (default \code{FALSE} - quiet). See \code{set_verbose_logging()}.}
}
\value{
\code{simulation_config}, wrapped under \code{monte_carlo} if it was flat.
}
\description{
\code{generate_monte_carlo_realizations()}'s own \verb{@examples} have always shown
a FLAT \code{simulation_config} (e.g. \code{list(distribution_type = "normal", max_depth = 200)}),
but \code{get_monte_carlo_defaults()} nests every Monte Carlo setting under
\verb{$monte_carlo}, and \code{merge_configurations()} merges strictly by matching
key path - so a flat user config silently landed as a new, unused
top-level \code{config$distribution_type} and had NO effect at all (confirmed
by testing: \code{distribution_type = "normal"} passed flat left
\code{config$monte_carlo$distribution_type} at its default \code{"triangular"},
every time). This accepts either shape: already-nested
(\code{list(monte_carlo = list(...))}) configs pass through unchanged; a flat
config whose names match known \code{monte_carlo} settings gets wrapped.
}
