% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-ssurgo-simulate.R
\name{normalize_requested_properties}
\alias{normalize_requested_properties}
\title{Normalize a Requested-Properties Vector to \code{simulate_cokey_generalized()} Parameter Names}
\usage{
normalize_requested_properties(requested_properties)
}
\arguments{
\item{requested_properties}{\code{NULL}, or a character vector of property identifiers in any of the
accepted vocabularies.}
}
\value{
\code{NULL}, or a de-duplicated character vector in canonical \code{param_order} order.
}
\description{
Resolves a caller's mixed vocabulary (caller-facing ids like \code{"clay"}/\code{"bulk_density"}, SSURGO
stems like \code{"ph1to1h2o"}, \code{simulate_cokey_generalized()} output columns like \code{"clay_total"}, or
the internal parameter names themselves) into the fixed
\code{c("db","wr_3b","wr_15b","ilr1","ilr2","rfv","ph","cec","soc")} vocabulary that
\code{simulate_cokey_generalized()} gates its per-row \code{param_list} on.
}
\details{
Texture is a coupled all-or-nothing unit: any texture member (\code{sand}/\code{silt}/\code{clay} in any
spelling, or \code{ilr1}/\code{ilr2} directly) pulls in \strong{both} ILR axes, since
\code{simulate_cokey_generalized()} derives \code{ilr1}/\code{ilr2} jointly from one sand+silt+clay draw and
only ever adds them as a pair. The only intra-\code{simulate_cokey_generalized()} derivation is this
texture coupling - any future dependency (e.g. a property estimated from another) is expanded
here.

Total and idempotent: \code{normalize_requested_properties(normalize_requested_properties(x))} equals
\code{normalize_requested_properties(x)}, so callers (including \code{simulate_cokey_generalized()} itself)
can normalize freely. \code{NULL} in -> \code{NULL} out ("simulate everything", the unchanged default).
}
\keyword{internal}
