% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{optimize_gp_hyperparameters}
\alias{optimize_gp_hyperparameters}
\title{Optimize GP Hyperparameters}
\usage{
optimize_gp_hyperparameters(X, Y, n_folds = 5, gp_control = c(20, 10, 2))
}
\arguments{
\item{X}{Scaled predictor matrix (depths).}

\item{Y}{Response vector.}

\item{n_folds}{Number of cross-validation folds.}

\item{gp_control}{Passed through to every \code{GPfit::GP_fit()} call this function makes
(\code{k_fold_gp_cv()}'s per-fold fits, the winning-candidate refit, and the fallback/baseline
fits) - see \code{fit_individual_gp_model()}'s docs for why the default is much smaller than
\code{GP_fit()}'s own default. With the default 5 folds x 3 correlation candidates + a refit,
this is ~16 \code{GP_fit()} calls per invocation - the single highest-multiplier fix in the
package's performance audit (see PERFORMANCE_IMPROVEMENT_PLAN.md).}
}
\value{
A \code{"GP"}-classed \code{\link[GPfit:GP_fit]{GPfit::GP_fit()}} model, with a \code{cv_results}
attribute describing the cross-validation that selected it.
}
\description{
Performs real \code{n_folds}-fold cross-validation comparing a small set of
\code{\link[GPfit:GP_fit]{GPfit::GP_fit()}} correlation-family specs (exponential power=1.95,
Matern nu=3/2, Matern nu=5/2 - the practically tunable surface GPfit
exposes), picks the lowest mean cross-validated RMSE, and refits on the
full data with the winning spec. CV results (folds requested, candidates
compared, winning candidate, per-candidate mean RMSE) are attached as the
\code{cv_results} attribute on the returned model so the search performed is
inspectable without changing the model's class/shape.
}
