% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-sampling.R
\name{percentile_sampling}
\alias{percentile_sampling}
\title{Flexible Percentile-Based Distribution Sampling}
\description{
Consolidates several "simulate a distribution from summary
percentiles" approaches (a piecewise-linear inverse-CDF, a monotonic-spline
inverse-CDF, a KDE/truncated-normal approach, and beta/normal parametric
fits) behind a single \code{method} argument, for arbitrary percentile counts
(not just SSURGO's low/rep/high triplet). Ported from
\code{code_ref/brdf/distribution_fitting.R}. Complements, rather than
replaces, \code{R/distributions.R}'s \code{fit_percentile_triplet()}/
\code{quantile_from_fit()}, which is the production Monte Carlo engine's
fixed 3-point fitter.

\code{method = "metalog"} is deliberately \strong{not} ported: it would need the
\code{rmetalog} package, which \code{distributions.R}'s own header documents a
project decision to avoid entirely (validated hang/segfault history) -
soilSIM already has a dependency-free closed-form metalog fitter
(\code{fit_metalog_linear()}/\code{quantile_metalog_linear()}) that doesn't need
it.
}
