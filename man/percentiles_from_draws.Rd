% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-ssurgo-simulate.R
\name{percentiles_from_draws}
\alias{percentiles_from_draws}
\title{Compute Per-Mukey Percentile Rasters from Already-Simulated SSURGO Draws}
\usage{
percentiles_from_draws(
  mukey_raster,
  draws,
  property_id,
  probs = c(0.05, 0.25, 0.5, 0.75, 0.95)
)
}
\arguments{
\item{mukey_raster}{A categorical (factor) mukey \code{terra::SpatRaster} from
\code{fetch_ssurgo_mukey_raster()}.}

\item{draws}{A data frame from \code{simulate_ssurgo_mapunit_draws()}, for the same AOI/depth
window \code{mukey_raster} covers.}

\item{property_id}{One of \code{property_to_sim_column()}'s recognized ids.}

\item{probs}{Percentile probabilities to compute (default \code{c(0.05, 0.25, 0.5, 0.75, 0.95)}).}
}
\value{
\verb{list(values = <named list of percentile-value SpatRasters>, probs = probs)}, or \code{NULL}
if \code{property_id}'s simulated column isn't present in \code{draws}.
}
\description{
The quantile/rasterize half of \code{\link[=fetch_ssurgo_percentiles]{fetch_ssurgo_percentiles()}}, factored out so a single shared
\code{\link[=simulate_ssurgo_mapunit_draws]{simulate_ssurgo_mapunit_draws()}} result can be reused across multiple properties instead of
resimulating once per property - \code{simulate_cokey_generalized()} already simulates every
recognized property jointly in one pass per cokey, so a caller that needs several properties
from the same AOI/depth window (e.g. \code{run_fusion_group()}'s texture members) only needs
to run the (expensive) simulation once and call this per property afterward.
}
\seealso{
Other raster-fusion: 
\code{\link{align_percentile_probs}()},
\code{\link{build_cache_key}()},
\code{\link{cache_get}()},
\code{\link{cache_set}()},
\code{\link{check_metalog_feasibility_raster}()},
\code{\link{fit_beta_mle_newton_raster}()},
\code{\link{fit_beta_mom_raster}()},
\code{\link{fit_gamma_mom_raster}()},
\code{\link{fit_metalog_linear_raster}()},
\code{\link{fit_normal_raster}()},
\code{\link{fuse_adaptive}()},
\code{\link{quantile_metalog_linear_with_fallback_raster}()},
\code{\link{remarginalize_awc}()},
\code{\link{remarginalize_ensemble_to_posterior}()},
\code{\link{resolve_property_dist}()},
\code{\link{wrap_nested_rasters}()}
}
\concept{raster-fusion}
\keyword{internal}
