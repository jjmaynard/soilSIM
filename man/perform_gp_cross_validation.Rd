% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-diagnostics.R
\name{perform_gp_cross_validation}
\alias{perform_gp_cross_validation}
\title{Perform GP Cross-Validation}
\usage{
perform_gp_cross_validation(gp_models, training_data, criteria)
}
\arguments{
\item{gp_models}{GP models from \code{build_stratified_gp_models()}.}

\item{training_data}{Original training data with \code{hzdept_r} and one
column per property in \code{gp_models}.}

\item{criteria}{List, optionally with \code{n_folds} (default 5).}
}
\value{
List with \code{cv_rmse}, \code{cv_r_squared}, \code{cv_quality}, \code{cv_method}.
}
\description{
Runs real k-fold cross-validation (reusing the shared \code{\link[=k_fold_gp_cv]{k_fold_gp_cv()}}
helper from \code{gp-modeling.R}) for each property in \code{gp_models} against the
matching column of \code{training_data}, aggregating mean CV RMSE and an
R-squared derived from it.
}
