% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{predict.soilSIM_gp_models}
\alias{predict.soilSIM_gp_models}
\title{Predict a fitted GP depth-trend model at new depths}
\usage{
\method{predict}{soilSIM_gp_models}(object, new_depths, property, group = NULL, ...)
}
\arguments{
\item{object}{A \code{soilSIM_gp_models} object from \code{\link[=fit_depth_gp_models]{fit_depth_gp_models()}}.}

\item{new_depths}{Numeric vector of depths (cm) to predict at.}

\item{property}{Which property's model to use (a name in \code{object}).}

\item{group}{Which stratified group's model to use (default: the first group fitted for
\code{property}, with a message when more than one exists - pass explicitly to silence it).}

\item{...}{Passed to \code{\link[=predict_gp_depth_trends]{predict_gp_depth_trends()}}.}
}
\value{
Numeric vector of predicted means, one per \code{new_depths} (see
\code{\link[=predict_gp_depth_trends]{predict_gp_depth_trends()}}'s own \verb{@return} - it is the fitted mean only, not a prediction
interval).
}
\description{
A convenience wrapper over \code{\link[=predict_gp_depth_trends]{predict_gp_depth_trends()}} (unchanged - this does not replace it,
since that function's real callers throughout the package operate on one leaf model at a time,
not the whole \code{soilSIM_gp_models} collection) that looks up the requested property/group's
fitted model inside a \code{\link[=fit_depth_gp_models]{fit_depth_gp_models()}} result.
}
