% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte-carlo.R
\name{prepare_simulation_parameters}
\alias{prepare_simulation_parameters}
\title{Prepare Simulation Parameters (Enhanced)}
\usage{
prepare_simulation_parameters(
  simulation_data,
  properties,
  config,
  composition_plan = NULL,
  verbose = getOption("ssurgo.verbose", FALSE)
)
}
\arguments{
\item{simulation_data}{Filtered simulation data}

\item{properties}{Properties to extract parameters for (may include
composition-group pseudo-properties)}

\item{config}{Configuration settings}

\item{composition_plan}{Optional result of \code{resolve_composition_groups()}.}

\item{verbose}{Logical; if \code{TRUE}, temporarily raises the package's
log level so \code{INFO}-level progress messages print for the duration
of this call (default \code{FALSE} - quiet). See \code{set_verbose_logging()}.}
}
\value{
Enhanced simulation parameters
}
\description{
Enhanced parameter preparation. Composition-group pseudo-properties (e.g.
\code{"ilr1"}/\code{"ilr2"}) are special-cased: BOTH are fit together, once per
horizon, via \code{distributions.R}'s \code{estimate_ilr_moments_mc()} from the
group's real member \verb{_l/_r/_h} triplets - their joint covariance only
makes sense computed jointly, not by looping \code{extract_property_parameters()}
per pseudo name. The Monte Carlo covariance's ilr1<->ilr2 OFF-DIAGONAL is
intentionally discarded (only the marginal SDs are kept): the cross-horizon
empirical correlation-matrix step (\code{estimate_property_correlations()}) owns
ilr1<->ilr2 correlation instead, so the two sources of correlation
(per-observation uncertainty propagation vs. cross-sample empirical
correlation) are never double-counted.
}
