% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{qnorm_percentiles_raster}
\alias{qnorm_percentiles_raster}
\title{Analytic Normal percentiles, raster-native (\code{mu + sigma * qnorm(p)})}
\usage{
qnorm_percentiles_raster(mu, sigma, posterior_probs)
}
\arguments{
\item{mu, sigma}{Normal parameter SpatRasters.}

\item{posterior_probs}{Numeric probabilities (0-1).}
}
\value{
Named list of percentile \code{SpatRaster}s (names \code{"P1"}, \code{"P50"}, ... matching \code{posterior_probs}).
}
\description{
Pure raster arithmetic - \code{qnorm(p)} is a single scalar per requested probability (unlike
\code{qbeta()}/\code{qgamma()}, whose quantile depends on the shape parameters too, not just \code{p}), so no
per-cell dispatch (\code{terra::app()}) is needed at all; exact, and effectively free.
}
\keyword{internal}
