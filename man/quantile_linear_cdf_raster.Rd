% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution-fitting-raster.R
\name{quantile_linear_cdf_raster}
\alias{quantile_linear_cdf_raster}
\title{Evaluate the piecewise-linear inverse-CDF at a fixed quantile \code{q} across a
raster. Because the percentile breakpoints (\code{probs}) are the same at
every cell, only the two value-rasters bracketing \code{q} and a single scalar
interpolation weight are needed - no per-cell branching required since
the bracket is resolved once, outside the raster arithmetic.}
\usage{
quantile_linear_cdf_raster(value_rasters, probs, q)
}
\arguments{
\item{value_rasters}{List of SpatRasters, sorted the same way as \code{probs}.}

\item{probs}{Numeric probabilities matching \code{value_rasters}' order.}

\item{q}{Target quantile probability.}
}
\description{
Evaluate the piecewise-linear inverse-CDF at a fixed quantile \code{q} across a
raster. Because the percentile breakpoints (\code{probs}) are the same at
every cell, only the two value-rasters bracketing \code{q} and a single scalar
interpolation weight are needed - no per-cell branching required since
the bracket is resolved once, outside the raster arithmetic.
}
