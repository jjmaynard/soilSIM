% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution-fitting-raster.R
\name{quantile_metalog_linear_with_fallback}
\alias{quantile_metalog_linear_with_fallback}
\title{Metalog quantile with automatic fallback to \code{linear_cdf} for infeasible
cells - validated upstream: zero effect on feasible cells, exact
\code{linear_cdf} match on infeasible ones.}
\usage{
quantile_metalog_linear_with_fallback(
  fit,
  infeasible_r,
  full_value_rasters,
  full_probs,
  q,
  bounds,
  boundedness
)
}
\arguments{
\item{fit}{Output of \code{fit_metalog_linear_raster()} (fit on INTERIOR percentiles).}

\item{infeasible_r}{Output of \code{check_metalog_feasibility_raster()} for this same \code{fit}.}

\item{full_value_rasters, full_probs}{The FULL percentile set (including
p=0/p=1 if available) - \code{linear_cdf} has no restriction there and
benefits from every available knot.}

\item{q}{Target quantile probability.}

\item{bounds, boundedness}{Same as \code{fit_metalog_linear_raster()}'s arguments.}
}
\value{
list(value = blended quantile raster, used_fallback = infeasible_r verbatim)
}
\description{
Feasibility is computed ONCE per fit (via \code{check_metalog_feasibility_raster()})
and passed in rather than recomputed per call, since it depends only on
the fitted coefficients, not on which quantile \code{q} is being evaluated.
}
