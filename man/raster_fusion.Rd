% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{raster_fusion}
\alias{raster_fusion}
\title{Raster-Native Bayesian Fusion (Prior x Likelihood, per Cell)}
\description{
Per-cell Bayesian fusion of a prior belief distribution
(e.g. an SSURGO-derived percentile raster set) with a likelihood (e.g.
an independent percentile raster set from another source), across a
whole \code{terra::SpatRaster} AOI at once. This is the raster-native
counterpart of \code{R/core-fusion.R}.

Uses \code{core-fusion.R}'s scalar/vector fusion functions
directly - \code{fuse_normal_normal()},
\code{fuse_beta()}, \code{fuse_gamma()}, \code{convert_moments_to_gamma()}/\code{convert_moments_to_beta()}/
\code{convert_beta_to_moments()}/\code{convert_gamma_to_moments()}, \code{convert_normal_to_lognormal()}/
\code{convert_lognormal_to_normal()}, \code{update_prior()}, and
\code{R/core-distributions.R}'s \code{estimate_ilr_moments_mc()}/\code{ilr_inverse()}, plus
\code{fuse_bivariate_normal()} - are all pure elementwise arithmetic, so they
work unchanged on \code{SpatRaster} inputs.
}
\section{Entry points}{

\code{run_fusion()}/\code{run_fusion_group()} are the top-level
orchestrators, wiring the fetch-and-cache layer (\code{R/cache.R},
\code{R/adapter-ssurgo-simulate.R}, \code{R/adapter-solus.R}) to the fusion core.
\code{fuse_property_adaptive()} is the lower-level entry point for callers
who already have their own pre-fetched \code{prior_value_rasters}/
\code{lik_value_rasters} and want to skip the fetch-and-cache wrapper.
}

\section{Configuration}{

\code{group_members()}/\code{fuse_property_adaptive()} take a per-call config
list/member vector directly, reusing soilSIM's
\code{config$monte_carlo$composition_groups} convention (see
\code{R/core-distributions.R}'s \code{resolve_composition_groups()}).
}

