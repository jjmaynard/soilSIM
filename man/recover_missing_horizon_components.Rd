% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo-acquisition.R
\name{recover_missing_horizon_components}
\alias{recover_missing_horizon_components}
\title{Recover SSURGO Components Missing All Horizon Data}
\usage{
recover_missing_horizon_components(
  ssurgo_data,
  all_components,
  verbose = FALSE
)
}
\arguments{
\item{ssurgo_data}{The AOI's already fully processed horizon-grain data frame (i.e.
\code{download_ssurgo_tabular()}'s Step 7 output - after RFV aggregation and restriction
indicators, before \code{validate_data_quality()}).}

\item{all_components}{Data frame from \code{fetch_ssurgo_all_components_working()} (or shaped like
it) - every real component for this AOI's \code{mukey_list}, independent of horizon data
availability.}

\item{verbose}{Logical; provide progress messages.}
}
\value{
\verb{list(ssurgo_data = <ssurgo_data, with synthesized rows appended if any>, components_missing_horizons = <data frame: mukey/cokey/compname/comppct_l/r/h for components neither present in ssurgo_data nor resolvable via an AOI sibling>, components_recovered = <data frame: mukey/cokey/compname/n_sibling_cokeys_used for components that WERE synthesized>)}.
}
\description{
\code{execute_ssurgo_query_working()}'s \verb{INNER JOIN chorizon} silently drops any real component that
has zero \code{chorizon} rows in SDA. This function finds such components (via \code{all_components}, a
component-only fetch with no chorizon join - see \code{fetch_ssurgo_all_components_working()}) and,
for each one, tries to synthesize a representative horizon profile by averaging OTHER cokeys in
the same AOI's \code{ssurgo_data} that share the missing component's \code{compname} (see
\code{synthesize_component_horizons_from_siblings()}). Components with no such AOI sibling are left
out of \code{ssurgo_data} (same as current behavior) but are returned in
\code{components_missing_horizons} instead of vanishing untraced.
}
\keyword{internal}
