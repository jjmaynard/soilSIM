% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{resolve_composition_groups}
\alias{resolve_composition_groups}
\title{Expand a requested properties vector around active composition groups}
\usage{
resolve_composition_groups(properties, config)
}
\arguments{
\item{properties}{Character vector of requested property names.}

\item{config}{A Monte Carlo config list; reads
\code{config$monte_carlo$composition_groups} (a named list, each entry
\code{list(members=, pseudo=)}).}
}
\value{
\code{list(sim_properties=, groups=)} where \code{groups} is a named list of
\code{list(members=, pseudo=, active=)}.
}
\description{
If ALL members of a configured composition group (e.g. \code{"texture"}:
clay/sand/silt) are present in \code{properties}, they are replaced (position
of the first member preserved) with the group's pseudo-property names
(e.g. \code{"ilr1"}/\code{"ilr2"}) so they can flow through the existing generic
Cholesky-copula simulation machinery like any other property. If only
some members are present, the group stays inactive (a logged WARN) and
those properties simulate independently via the legacy path.
}
\details{
\code{members} declares which real property occupies \code{ilr_forward()}/
\code{ilr_inverse()}'s position 1/2/3 (their \code{clay}/\code{sand}/\code{silt} naming is a
positional-role placeholder, not an identity requirement - see the header
comment in the ILR section of this file). \code{restore_composition_properties()}
maps \code{ilr_inverse()}'s fixed \code{clay}/\code{sand}/\code{silt} output columns onto
\code{members[1]}/\code{members[2]}/\code{members[3]} by POSITION, not by name matching.
}
