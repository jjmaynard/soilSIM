% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{resolve_property_dist}
\alias{resolve_property_dist}
\title{Resolve a property's distribution family, refining \code{dist = "auto"} from
the AOI's own percentile skew rather than a fixed per-property label.}
\usage{
resolve_property_dist(
  property_config,
  prior_value_rasters,
  prior_probs,
  aggregate_fun = stats::median
)
}
\arguments{
\item{property_config}{List with \code{dist} and optionally \code{bounds}/\code{auto_skew_threshold}.}

\item{prior_value_rasters, prior_probs}{Prior percentile-value rasters/probabilities.}

\item{aggregate_fun}{Function to aggregate the AOI-wide skew proxy (default \code{stats::median}).}
}
\value{
\verb{list(dist=, dist_source= one of "config"/"auto", skew_proxy= NA_real_ unless dist_source=="auto")}.
}
\description{
Computes a skewness proxy directly from the AOI's own percentile triplet:
\code{((high-median)-(median-low))/(high-low)}, aggregated once across the AOI
(default \code{median}) - NOT per-cell, which would create map discontinuity
artifacts at family-boundary cells.
}
\details{
Deliberately narrow: only ever resolves to "beta" (if \code{bounds} is
configured), "normal", or "lognormal" - never "metalog" or "gamma",
since distinguishing those from a 3-point proxy isn't reliable. NEVER
overrides an explicit (non-"auto") \code{dist} value.
}
