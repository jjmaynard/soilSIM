% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel-utils.R
\name{run_parallel_lapply}
\alias{run_parallel_lapply}
\title{Run a Function Over a List/Vector, in Parallel via \code{future.apply}, with Sequential Fallback}
\usage{
run_parallel_lapply(
  X,
  FUN,
  ...,
  n_cores = NULL,
  future_seed = FALSE,
  op_name = "Parallel operation",
  sequential_fallback = NULL,
  catch_errors = TRUE
)
}
\arguments{
\item{X}{A list or vector to iterate over.}

\item{FUN}{A function taking one element of \code{X} as its first argument, plus \code{...}.}

\item{...}{Additional fixed arguments forwarded to \code{FUN} on every call.}

\item{n_cores}{Number of parallel workers. \code{NULL} (default) resolves to
\code{max(1, parallel::detectCores() - 1)}, then is clamped to \code{min(n_cores, length(X))} (no point
starting more workers than there is work). A resolved value \verb{<= 1} skips \code{future::plan()}
setup entirely and just runs \code{lapply(X, FUN, ...)}.}

\item{future_seed}{Passed through to \code{future.apply::future_lapply()}'s \code{future.seed} argument.
Use \code{TRUE} whenever \code{FUN} (or anything it calls) uses R's RNG, to get \code{future}'s
parallel-safe RNG streams - this is more callers than it first appears: besides the obvious
cases (Monte Carlo draws, profile perturbation), \code{GPfit::GP_fit()}'s internal hyperparameter
search is a genetic algorithm and genuinely uses RNG too (confirmed empirically via
\code{future_lapply()}'s "UNRELIABLE VALUE" warning when this was left \code{FALSE} for a GP-fitting
caller in this package), even though its fitted result is highly stable across runs. Default
\code{FALSE} matches \code{future_lapply()}'s own default; only leave it \code{FALSE} for \code{FUN} you've
confirmed doesn't touch the RNG anywhere in its call chain.}

\item{op_name}{A short label used in the log message and as \code{handle_workflow_error()}'s
context string if dispatch fails.}

\item{sequential_fallback}{A zero-argument closure invoked if dispatch fails and \code{catch_errors = TRUE} (the caller pre-binds its own arguments, e.g. \code{function() run_sequential_simulation( simulation_params, ...)}). \code{NULL} (default) falls back to plain \code{lapply(X, FUN, ...)} instead.
Ignored when \code{catch_errors = FALSE}.}

\item{catch_errors}{If \code{TRUE} (default), wraps dispatch in \code{tryCatch()} and applies the
\code{sequential_fallback} contract above on error. If \code{FALSE}, dispatch errors propagate to the
caller's own error handling instead (plan setup/restoration via \code{on.exit()} still happens
regardless) - needed for callers whose own contract is "no sequential fallback, propagate/
convert the error myself" (e.g. \code{simulate_profile_depths_by_collection_parallel()}, which
returns \code{NULL} on error via its own outer \code{tryCatch()}).}
}
\value{
A list of \code{FUN(x, ...)} results, one per element of \code{X}, in \code{X}'s order - the same
shape \code{parallel::parLapply()}/\code{mclapply()} already produced, so callers that previously
consumed those results (e.g. via \code{dplyr::bind_rows()} or manual concatenation) need no change.
}
\description{
Run a Function Over a List/Vector, in Parallel via \code{future.apply}, with Sequential Fallback
}
\keyword{internal}
