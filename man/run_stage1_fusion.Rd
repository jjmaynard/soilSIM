% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{run_stage1_fusion}
\alias{run_stage1_fusion}
\title{Run Stage 1 Fusion for One Property/Depth over an AOI}
\usage{
run_stage1_fusion(
  aoi_vect,
  property_config,
  top_depth,
  bottom_depth,
  composition_groups = NULL,
  property_configs = NULL,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI (projected, e.g. EPSG:5070).}

\item{property_config}{A list with \code{id} (used as \code{fetch_ssurgo_percentiles()}'s property id and
as the cache key), \code{solus_variable} (a \code{soilDB::fetchSOLUS()}-recognized variable name), and
\code{dist}/\code{bounds}/\code{boundedness}/\code{auto_skew_threshold}/\code{composition_group} as needed by
\code{fuse_property_adaptive()}.}

\item{top_depth, bottom_depth}{Numeric depth bounds in cm.}

\item{composition_groups, property_configs}{Only needed when \code{property_config$composition_group}
is set - passed through to \code{run_stage1_fusion_group()} (see its docs).}

\item{parallel, n_cores}{Passed through to \code{simulate_ssurgo_mapunit_draws()}'s
\code{parallel}/\code{n_cores} (via \code{fetch_ssurgo_percentiles()} or \code{run_stage1_fusion_group()},
whichever path this call takes) - the SSURGO adapter's per-cokey depth-trend GP fitting is
this pipeline's dominant cost for AOIs with many cokeys. Default \code{parallel = FALSE} matches
prior behavior exactly.}
}
\value{
\code{list(prior=, likelihood=, posterior=, dist=, dist_source=, skew_proxy=, route=, route_detail=, n_fallback_cells=)}, or \code{NULL} if the SSURGO or SOLUS side failed.
\code{posterior}'s shape depends on \code{dist} - see \code{fuse_property_adaptive()}'s docs. As of
\code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md} tasks P3.2-P3.5, \code{posterior} also carries a
\code{percentiles} field (a named list of \code{SpatRaster}s, \code{"P01"}..\code{"P99"} by default - see
\link{FUSE_POSTERIOR_DEFAULT_PROBS}) regardless of \code{dist}, for every non-compositional property -
this function doesn't expose its own \code{posterior_probs} parameter (matching the existing
convention that \code{n_samples}/\code{grid_resolution} also aren't threaded this far down; the
underlying \verb{fuse_*()} route's own default applies).
}
\description{
Fetches the (cached) SSURGO prior (\code{R/ssurgo-simulation.R}'s \code{fetch_ssurgo_percentiles()}) and
SOLUS likelihood (\code{R/solus-simulation.R}'s \code{fetch_solus_percentiles()}) percentiles, aligns the
SSURGO grid onto the SOLUS grid via \code{terra::resample()}, then fuses via
\code{fuse_property_adaptive()}.
}
\details{
Compositional properties (\code{property_config$composition_group} set) are detected up front and
delegated to \code{run_stage1_fusion_group()} instead, since they must be fetched/fit jointly.
}
\section{SSURGO simulation scope}{

The SSURGO prior simulation is restricted to just this call's own property (plus the full
sand/silt/clay draw for any texture member) via \code{simulate_ssurgo_mapunit_draws()}'s
\code{requested_properties} - the per-cokey depth-trend GP fit is the pipeline's dominant cost and
scales with the property count. Consequences: (1) the prior is \emph{statistically equivalent} to,
not bit-identical to, a full-property simulation's matching column (a fresh unseeded draw either
way); (2) a map unit whose components carry no data for \emph{this} property no longer benefits from
those components' other properties being present, so a single-property prior - and hence the
fused posterior - can be \code{NA} for a few cells a full-property run would have covered. Those
cells genuinely lack data for the property in question.
}

\section{Fusion fidelity - \code{prior_fusion_method} (default changed at P2.10)}{

Controls whether fusion runs against each mukey's real per-cell Monte Carlo draws
(\code{"raw_draws"}) or a percentile-reconstructed approximation (\code{"percentile"}) - see
\code{fuse_general_kde()}'s docs and \code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md} tasks
P2.2/P2.3/P2.6/P2.10. \strong{As of P2.10, leaving \code{property_config$prior_fusion_method} unset
defaults to \code{"raw_draws"}} for \code{dist \%in\% c("auto","normal","beta","gamma","lognormal")} - see
\code{resolve_want_raw_draws()} for the exact rule and the benchmark numbers behind it. Only an
explicit \code{dist = "metalog"} keeps the pre-P2.10 default - \code{fuse_metalog_adapter()} does have a
raw-draws fit as of P2.12, but its cost/benefit is unbenchmarked (mirroring the texture-group
route's P2.11 treatment), so the default stays conservative until that's measured. Set
\code{property_config$prior_fusion_method = "percentile"} explicitly to opt back into the original
behavior for any \code{dist} (e.g. to match older cached/tested output, or avoid the live simulation
dependency); \code{"raw_draws"} explicitly forces it on even for \code{dist = "metalog"}, now genuinely
used there (P2.12), not just accepted-and-ignored.

The real draws are held in memory only for the duration of this one call, never disk-cached
(see \code{simulate_ssurgo_mapunit_draws()}'s docs for why) - so raw_draws fusion (whether defaulted
or explicit) forces a fresh simulation even when this AOI/property/depth-window's \code{"ssurgo"}
percentile cache is already warm (the draws that produced that cached percentile summary weren't
kept). That simulation still runs at most once per call regardless of cache state - it is shared
with the percentile-cache-population step when both are needed in the same call.
}

