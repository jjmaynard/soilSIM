% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{run_stage1_fusion_group}
\alias{run_stage1_fusion_group}
\title{Run Stage 1 Fusion for a Whole Compositional Group Jointly}
\usage{
run_stage1_fusion_group(
  aoi_vect,
  group,
  composition_groups,
  property_configs,
  top_depth,
  bottom_depth,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{group}{A composition group name (e.g. \code{"texture"}).}

\item{composition_groups}{A \code{config$monte_carlo$composition_groups}-shaped list (see
\code{group_members()}) giving this group's member ids in order.}

\item{property_configs}{A named list, keyed by each member id \code{composition_groups} lists,
of per-member config lists (each needs at least \code{id}/\code{solus_variable}) - the per-call
replacement for the original bundle's global \code{PROPERTIES} registry.}

\item{top_depth, bottom_depth}{Numeric depth bounds in cm.}

\item{parallel, n_cores}{Passed through to \code{simulate_ssurgo_mapunit_draws()}'s
\code{parallel}/\code{n_cores} for the shared draws computation below (only relevant when at least one
member isn't already disk-cached). Default \code{parallel = FALSE} matches prior behavior exactly.}
}
\value{
The full group result: a named list keyed by member id, each element
\code{list(posterior = list(value=, ilr_mu=, ilr_Sigma=), dist = "texture_ilr", route = "closed_form_ilr_group", route_detail = NULL, n_fallback_cells = 0)} (see
\code{fuse_texture_group()}'s docs - NOT the uniform \verb{(mu,sigma)}/\verb{(alpha,beta)} contract
non-compositional properties get) - or \code{NULL} if any member's SSURGO/SOLUS fetch failed.
\code{run_stage1_fusion()}'s own dispatch slices this down to the single requested member
(\code{group_result[[property_config$id]]}) to keep its own per-property return contract
consistent regardless of \code{dist}.
}
\description{
Fuses a compositional group's members (currently only \code{"texture"}: clay/sand/silt) jointly via
\code{fuse_texture_group()}, instead of one independent \code{run_stage1_fusion()} call per member -
independent fusion measurably breaks sum-to-100. Each member's SSURGO/SOLUS percentiles are
still fetched/cached per-property (so a later request for just one member's raw percentiles
still hits cache), but the fusion itself runs once for the whole group, under a \code{"texture_group"}
cache kind, with each member's resulting posterior also seeded into a per-property \code{"posterior"}
cache kind - so three sequential \code{run_stage1_fusion()} calls for clay, then sand, then silt
trigger the joint fetch+fusion exactly once, not three times.
}
