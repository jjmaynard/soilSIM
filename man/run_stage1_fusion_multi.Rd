% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{run_stage1_fusion_multi}
\alias{run_stage1_fusion_multi}
\title{Run Stage 1 Fusion for Many Properties over an AOI in One Simulation Pass}
\usage{
run_stage1_fusion_multi(
  aoi_vect,
  property_configs,
  depth_windows,
  composition_groups = NULL,
  n_mc = 1000,
  parallel = FALSE,
  n_cores = NULL,
  verbose = FALSE,
  simplify = FALSE
)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI (projected, e.g. EPSG:5070).}

\item{property_configs}{A \strong{named} list, keyed by each config's own \code{id}, of \code{property_config}
lists exactly as \code{\link[=run_stage1_fusion]{run_stage1_fusion()}} takes them (each needs \code{id}/\code{solus_variable}, optionally
\code{dist}/\code{bounds}/\code{composition_group}/\code{prior_fusion_method}/...). Configs carrying a
\code{composition_group} are fused jointly per \code{\link[=run_stage1_fusion_group]{run_stage1_fusion_group()}}; \strong{every} member of any
referenced group must have its own entry here.}

\item{depth_windows}{A non-empty list of \code{c(top, bottom)} numeric pairs (cm, \code{bottom > top}).}

\item{composition_groups}{A \code{config$monte_carlo$composition_groups}-shaped list (see
\code{\link[=group_members]{group_members()}}); required if any config sets \code{composition_group}.}

\item{n_mc, parallel, n_cores}{Passed to \code{\link[=simulate_ssurgo_mapunit_draws]{simulate_ssurgo_mapunit_draws()}}.}

\item{verbose}{Passed to \code{\link[=fuse_property_adaptive]{fuse_property_adaptive()}} - default \code{FALSE} (a multi-leaf batch is
otherwise noisy with per-route messages).}

\item{simplify}{If \code{TRUE}, each leaf is reduced to \verb{list(percentiles = <named SpatRasters>)}
(what per-pixel re-marginalization consumes); default \code{FALSE} returns the full
\code{\link[=run_stage1_fusion]{run_stage1_fusion()}}-shaped list per leaf.}
}
\value{
A nested named list \code{result[[config_id]][["<top>-<bottom>"]]}. Each leaf is a
\code{\link[=run_stage1_fusion]{run_stage1_fusion()}} return list (or, for group members, the \code{texture_ilr}-shaped list from
\code{\link[=run_stage1_fusion_group]{run_stage1_fusion_group()}}), or \code{NULL} on that leaf's own failure; the whole call returns
\code{NULL} only if the shared simulation itself fails.
}
\description{
The multi-property / multi-depth-window counterpart of \code{\link[=run_stage1_fusion]{run_stage1_fusion()}}: runs the
expensive SSURGO Monte Carlo simulation \strong{once} - covering every requested property and every
depth window via \code{\link[=simulate_ssurgo_mapunit_draws]{simulate_ssurgo_mapunit_draws()}}'s \code{depth_windows} argument - then fuses each
property/window against SOLUS, seeding the same per-property \code{"ssurgo"}/\code{"solus"}/\code{"posterior"}
disk caches \code{\link[=run_stage1_fusion]{run_stage1_fusion()}} reads. A later single-property \code{run_stage1_fusion()} call for
the same AOI/property/window therefore hits cache instead of re-simulating.
}
\details{
Calling \code{run_stage1_fusion()} in a loop over N properties x M windows runs the (dominant-cost)
per-cokey simulation N*M times, each discarding all but one of the ~9 jointly-simulated
properties; this runs it once. Every leaf shares that one draw set, so - unlike independent
\code{run_stage1_fusion()} calls - their \code{NA} masks are mutually consistent.
}
\seealso{
\code{\link[=run_stage1_fusion]{run_stage1_fusion()}}, \code{\link[=run_stage1_fusion_group]{run_stage1_fusion_group()}}, \code{\link[=extract_mukey_joint_ensemble]{extract_mukey_joint_ensemble()}}
}
