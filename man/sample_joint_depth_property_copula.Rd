% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate-adjustment.R
\name{sample_joint_depth_property_copula}
\alias{sample_joint_depth_property_copula}
\title{Draw a Joint Depth x Property Gaussian Copula Sample}
\usage{
sample_joint_depth_property_copula(R_depth, R_prop, n_sims, seed = NULL)
}
\arguments{
\item{R_depth}{An \verb{n_depths x n_depths} correlation matrix (e.g. from
\code{build_depth_correlation_kernel()}).}

\item{R_prop}{A \verb{k x k} correlation matrix (the same flat property-correlation matrix
\code{simulate_correlated_triangular()} already uses for within-horizon correlation).}

\item{n_sims}{Number of joint realizations to draw.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \code{c(n_depths, k, n_sims)} array of standard-normal values (mean 0, variance 1
marginally), jointly correlated across both the depth and property dimensions as specified.
}
\description{
Draws \code{n_sims} realizations of an \verb{n_depths x k} standard-normal field whose ROWS (depths) are
correlated according to \code{R_depth} and whose COLUMNS (properties) are correlated according to
\code{R_prop}, SIMULTANEOUSLY - the standard separable/Kronecker-structured multivariate-normal
sampling identity \code{Z = L_depth \%*\% Eps \%*\% t(L_prop)} (where \code{L_depth}/\code{L_prop} are Cholesky
factors of \code{R_depth}/\code{R_prop} and \code{Eps} is iid standard normal), which achieves
\verb{Cov(vec(Z)) = R_prop (x) R_depth} (a Kronecker product) without ever materializing that full
\verb{(n_depths*k) x (n_depths*k)} matrix. Vectorized across all \code{n_sims} realizations at once via
array reshaping (no explicit per-realization loop).
}
