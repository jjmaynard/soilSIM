% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gp-modeling.R
\name{select_simulation_correlation_matrix}
\alias{select_simulation_correlation_matrix}
\title{Select a Correlation Matrix and Property Set for Single-Cokey Simulation}
\usage{
select_simulation_correlation_matrix(
  cokey_data,
  correlation_matrices,
  txt_correlation_matrices
)
}
\arguments{
\item{cokey_data}{Simulation data for a single cokey.}

\item{correlation_matrices}{A matrix, a genhz-keyed named list of
matrices, or \code{NULL}.}

\item{txt_correlation_matrices}{Same shape as \code{correlation_matrices}, for
texture properties; used as a fallback when \code{correlation_matrices} is
unusable.}
}
\value{
List with \code{properties} (character vector) and \code{correlation_matrix}
(a matrix or \code{NULL}, letting \code{\link[=generate_monte_carlo_realizations]{generate_monte_carlo_realizations()}}
estimate one empirically).
}
\description{
Chooses a single flat correlation matrix (and its associated property set)
to hand to \code{\link[=generate_monte_carlo_realizations]{generate_monte_carlo_realizations()}} for one cokey's data,
from either a plain matrix or a genhz-keyed list of matrices.
}
