% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{set_verbose_logging}
\alias{set_verbose_logging}
\title{Temporarily raise the package's log verbosity for the duration of a call}
\usage{
set_verbose_logging(verbose, raised_level = "INFO")
}
\arguments{
\item{verbose}{Logical.}

\item{raised_level}{Log level to switch to when \code{verbose = TRUE} (default \code{"INFO"} - matches
the package's historical visible-by-default narration; \code{"DEBUG"}-level detail remains
available only via an explicit \code{setup_logging(log_level = "DEBUG")} call, not through this
per-call \code{verbose} mechanism).}
}
\value{
Invisibly, the log config in effect \emph{before} this call (pass to \code{on.exit()} to
restore it).
}
\description{
Call at the top of a function with its own \code{verbose} argument, immediately followed by
\verb{on.exit(options(soil_workflow_log_config = <the returned value>), add = TRUE)}. When
\code{verbose = TRUE}, raises the shared \code{soil_workflow_log_config} option's \code{log_level} to
\code{raised_level} for the remainder of the calling function's execution (restored via the
caller's own \code{on.exit()}), so every \code{\link[=log_message]{log_message()}} call made anywhere during that call -
including deep inside shared helpers that have no \code{verbose} parameter of their own, and
across package/file boundaries - becomes visible. Only ever \emph{raises} the level, never
lowers it, so nested calls compose correctly regardless of order: an outer \code{verbose = TRUE}
makes everything below it visible even if an inner helper's own \code{verbose} is \code{FALSE}, and an
inner call's own restore is a harmless no-op when the level was already raised by an outer
caller. When \code{verbose = FALSE} (the default everywhere), does nothing - the ambient default
(see \code{\link[=log_message]{log_message()}}) is already quiet.
}
\keyword{internal}
