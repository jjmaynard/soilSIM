% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/property-simulation.R
\name{sim_component_comp}
\alias{sim_component_comp}
\title{Simulate Soil Component Composition}
\usage{
sim_component_comp(data, n_simulations = 1000)
}
\arguments{
\item{data}{Data frame with \code{mukey}, \code{cokey}, \code{compname}, \code{comppct_l}, \code{comppct_r}, \code{comppct_h}
(one or more rows per component; deduplicated internally).}

\item{n_simulations}{Integer, number of triangular draws per component (default 1000).}
}
\value{
A data frame, one row per component, with an added \code{sim_comppct} column.
}
\description{
Simulates component-composition percentages from low/rep/high (\code{comppct_l/r/h}) values via
\code{tri_dist()}, one component (\code{cokey}) at a time, and derives a single \code{sim_comppct} value per
component.
}
\section{Grain mismatch with \code{R/depth-simulation.R}}{

This returns one row per \strong{component} (\code{cokey}), but \code{simulate_and_perturb_soil_profiles()}/
\code{simulate_profile_depths_by_mukey()} need \code{sim_comppct} on every \strong{horizon} row. Callers must
\code{dplyr::left_join()} this output onto horizon-level data by \code{cokey} before passing it to those
functions - this function alone does not satisfy their requirement.
}

\section{\code{sim_comppct}'s derivation is unusual - documented, not "fixed"}{

\verb{sim_comppct <- round(sum(<n_simulations> triangular draws of comppct) / 100)} - this is
\code{round(n_simulations * comppct_r / 100)} in expectation (e.g. \code{comppct_r = 30},
\code{n_simulations = 1000} -> \code{sim_comppct} ~= 300), not an independently-meaningful simulation
count. Ported verbatim (preserve-behavior convention) since the legacy pipeline's exact intent
for this derivation isn't independently confirmable from this file alone - do not assume it
means something more sensible than what's written here.
}

