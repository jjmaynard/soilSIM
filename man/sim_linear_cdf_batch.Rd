% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-sampling.R
\name{sim_linear_cdf_batch}
\alias{sim_linear_cdf_batch}
\title{Batched piecewise-linear inverse-CDF sampler across many rows sharing the same \code{probs} knots}
\usage{
sim_linear_cdf_batch(probs, values_mat, n)
}
\arguments{
\item{probs}{Numeric probabilities (0-1), sorted ascending, shared by every row.}

\item{values_mat}{Numeric matrix, \code{nrow(values_mat)} rows x \code{length(probs)} columns (column
order matching \code{probs} order), no \code{NA}/missing values.}

\item{n}{Number of draws per row.}
}
\value{
A \code{nrow(values_mat)} x \code{n} numeric matrix, one row of draws per input row.
}
\description{
Vectorized equivalent of calling \code{sim_linear_cdf(probs, values_mat[i, ], n)} once per row of
\code{values_mat} and rbind-ing the results - built for \code{fuse_general_kde()} (\code{R/raster-fusion.R}),
where \code{stats::approxfun()} was previously rebuilt once per raster cell (\code{Rprof()} profiling
attributed ~9\% of \code{fuse_general_kde()}'s total wall-clock to \code{extract_percentile_pairs()}'s
per-cell dispatch overhead alone, on top of the sampling itself - see
PERFORMANCE_IMPROVEMENT_PLAN.md Tier 4). All rows must share the same \code{probs} knots (true for a
raster chunk, where every cell's percentile columns are the same fixed set, e.g. P5/P50/P95) -
this is what makes batching valid; per-row-varying \code{probs} would need the per-row \code{approxfun()}
approach this function replaces.
}
\details{
Matches \code{sim_linear_cdf()}'s \code{rule = 2} constant-extrapolation behavior for draws outside
\verb{[min(probs), max(probs)]} (clamped to the first/last \code{values} column) exactly, but does NOT
preserve \code{sim_linear_cdf()}'s per-row RNG stream position - the two are statistically
equivalent (both draw \code{runif(n)} per row and linearly interpolate), not bit-identical, since
this function draws the full \code{nrow(values_mat) * n} uniform block in one call rather than one
\code{runif(n)} call per row.
}
\keyword{internal}
