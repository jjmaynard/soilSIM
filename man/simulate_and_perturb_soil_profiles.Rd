% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{simulate_and_perturb_soil_profiles}
\alias{simulate_and_perturb_soil_profiles}
\title{Simulate and Perturb Soil Profiles}
\usage{
simulate_and_perturb_soil_profiles(soil_profile)
}
\arguments{
\item{soil_profile}{A SoilProfileCollection object containing soil profile data.}
}
\value{
A SoilProfileCollection object with perturbed horizon depths.
}
\description{
Takes a \code{SoilProfileCollection} object and perturbs its horizons to
simulate variability in soil profile thickness and boundary depths. The
simulation is performed in two steps: first, horizon thickness is
perturbed based on simulated thickness variability (using a top-down or
bottom-up simulation method), and then the boundary depths are perturbed
using distinctness-derived offsets. If the profile contains only one
horizon, the function simply replicates the profile.
}
\section{Known limitation}{

This function requires \code{soil_profile}'s horizons to already carry a
\code{sim_comppct} column (it derives the number of simulations to run from
\code{unique(horizons(soil_profile)$sim_comppct)}). \code{sim_component_comp()}
(\code{R/property-simulation.R}) produces this column, but at component
(\code{cokey}) grain, not horizon grain - callers must
\code{dplyr::left_join(horizon_data, sim_component_comp(component_data), by = "cokey")}
before calling this function; it will still error with a missing-column
condition if that join hasn't been done first.
}

