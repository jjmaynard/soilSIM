% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/property-simulation.R
\name{simulate_cokey_generalized}
\alias{simulate_cokey_generalized}
\title{Simulate Soil Properties for a Specific Cokey (Generalized)}
\usage{
simulate_cokey_generalized(
  sim_cokey,
  correlation_matrices,
  txt_correlation_matrices = NULL
)
}
\arguments{
\item{sim_cokey}{A data frame of horizon rows for one cokey, with a \code{genhz} column and a
\code{sim_comppct} column (number of realizations to simulate for that row - see
\code{sim_component_comp()}).}

\item{correlation_matrices}{A list of correlation matrices keyed by \code{genhz}, with row/column
names matching (a subset of) \code{c("db", "wr_3b", "wr_15b", "ilr1", "ilr2", "rfv", "ph", "cec", "soc")}.}

\item{txt_correlation_matrices}{A list of texture correlation matrices keyed by \code{genhz}
(sand/silt/clay order, matching the \code{simulate_correlated_triangular()} call for texture).}
}
\value{
A data frame of simulated property values across all rows/realizations, with
\code{compname}, \code{mukey}, \code{cokey}, \code{hzdept_r}, \code{hzdepb_r}, \code{simulation_number}, \code{unique_id}.
}
\description{
Simulates a flexible set of soil properties (whichever of bulk density, water retention,
texture, rock fragment volume, pH, CEC, organic carbon have complete \verb{_l/_r/_h} triplets
present) for each row of a single cokey's horizon data, via
\code{simulate_correlated_triangular()}, using a genhz-keyed local correlation matrix. If texture
columns are present, sand/silt/clay are simulated jointly and converted to ILR coordinates
(\code{ilr1}/\code{ilr2}) before being included in the main correlated simulation, then converted back
after.
}
