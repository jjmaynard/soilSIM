% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/property-simulation.R
\name{simulate_cokey_generalized}
\alias{simulate_cokey_generalized}
\title{Simulate Soil Properties for a Specific Cokey (Generalized)}
\usage{
simulate_cokey_generalized(
  sim_cokey,
  correlation_matrices,
  txt_correlation_matrices = NULL,
  requested_properties = NULL
)
}
\arguments{
\item{sim_cokey}{A data frame of horizon rows for one cokey, with a \code{genhz} column and a
\code{sim_comppct} column (number of realizations to simulate for that row - see
\code{sim_component_comp()}).}

\item{correlation_matrices}{A list of correlation matrices keyed by \code{genhz}, with row/column
names matching (a subset of) \code{c("db", "wr_3b", "wr_15b", "ilr1", "ilr2", "rfv", "ph", "cec", "soc", "caco3", "ec", "ecec", "gypsum", "sar")}. The 5 chemistry properties added in
MULTI_PROPERTY_FUSION_PLAN.md task P2 have no real KSSL-fit correlation data (see
\code{build_kssl_fallback_matrix()}) - \code{simulate_ssurgo_mapunit_draws()} builds its
\code{correlation_matrices} through that function so they're present (as identity/uncorrelated)
rather than missing; a direct caller supplying its own matrix without them will simply never
populate \code{param_list[["caco3"]]} etc. (see \code{get_param_set()}'s per-row gating below), not error.}

\item{txt_correlation_matrices}{A list of texture correlation matrices keyed by \code{genhz}
(sand/silt/clay order, matching the \code{simulate_correlated_triangular()} call for texture).}

\item{requested_properties}{\code{NULL} (default) simulates every property with a complete \verb{_l/_r/_h}
triplet, unchanged. Otherwise a character vector of property identifiers - in any vocabulary
\code{normalize_requested_properties()} accepts (caller ids, SSURGO stems, output column names, or
the internal \code{param_order} names) - restricting the simulation to just those (plus the texture
coupling: any texture member pulls in the full sand/silt/clay draw and both ILR axes). The
Gaussian-copula marginals of a restricted simulation match the corresponding columns of a full
one under the default \code{joint_copula} vertical-correlation method (modulo RNG-stream Monte Carlo
noise); see \code{simulate_ssurgo_mapunit_draws()} for the \code{gp_quantile_retrofit} caveat.}
}
\value{
A data frame of simulated property values across all rows/realizations, with
\code{compname}, \code{mukey}, \code{cokey}, \code{hzdept_r}, \code{hzdepb_r}, \code{simulation_number}, \code{unique_id}, and
(when \code{sim_cokey} itself has a \code{bound_sd} column - see \code{attach_osd_boundary_distinctness()})
\code{bound_sd}. The \code{soc} column is an SSURGO-organic-matter-\strong{derived SOC estimate}
(\code{om * OM_TO_SOC_FACTOR}, the inverse Van Bemmelen factor) - not a lab-measured soil organic
carbon value. See \code{OM_TO_SOC_FACTOR}'s own docs (MULTI_PROPERTY_FUSION_PLAN.md task P1).
}
\description{
Simulates a flexible set of soil properties (whichever of bulk density, water retention,
texture, rock fragment volume, pH, CEC, organic carbon have complete \verb{_l/_r/_h} triplets
present) for each row of a single cokey's horizon data, via
\code{simulate_correlated_triangular()}, using a genhz-keyed local correlation matrix. If texture
columns are present, sand/silt/clay are simulated jointly and converted to ILR coordinates
(\code{ilr1}/\code{ilr2}) before being included in the main correlated simulation, then converted back
after.
}
