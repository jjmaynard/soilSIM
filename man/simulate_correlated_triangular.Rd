% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/property-simulation.R
\name{simulate_correlated_triangular}
\alias{simulate_correlated_triangular}
\title{Simulate Correlated Samples from Triangular Distributions}
\usage{
simulate_correlated_triangular(
  n,
  params,
  correlation_matrix,
  random_seed = NULL
)
}
\arguments{
\item{n}{Integer, number of samples to generate.}

\item{params}{List of \code{c(a, b, c)} triples, one per distribution - \strong{note the order here is
(lower, mode, upper)}, unlike \code{tri_dist()}'s own \verb{(a = lower, b = upper, c = mode)}
convention. Preserved exactly as the legacy source defines it; a real source of confusion if
assumed to match \code{tri_dist()}'s argument order.}

\item{correlation_matrix}{A square, positive-semi-definite correlation matrix,
\code{length(params)} x \code{length(params)}.}

\item{random_seed}{Optional integer seed for reproducibility.}
}
\value{
A matrix of correlated samples, \code{n} rows x \code{length(params)} columns.
}
\description{
Generates \code{n} correlated samples from \code{length(params)} triangular distributions via a
Cholesky decomposition of \code{correlation_matrix} applied to standard-normal draws, then
transforms each column to its own triangular distribution via the inverse CDF.
}
