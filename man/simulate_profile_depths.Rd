% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-simulation.R
\name{simulate_profile_depths}
\alias{simulate_profile_depths}
\alias{simulate_profile_depths.SoilProfileCollection}
\alias{simulate_profile_depths.default}
\title{Simulate soil profile depths, dispatching on input type}
\usage{
simulate_profile_depths(x, ...)

\method{simulate_profile_depths}{SoilProfileCollection}(x, seed = 123, parallel = FALSE, n_cores = 6, ...)

\method{simulate_profile_depths}{default}(x, n_simulations = 100, seed = 123, ...)
}
\arguments{
\item{x}{Either a \code{SoilProfileCollection} (\code{\link[=simulate_profile_depths.SoilProfileCollection]{simulate_profile_depths.SoilProfileCollection()}}) or
a mukey string/number (\code{\link[=simulate_profile_depths.default]{simulate_profile_depths.default()}}, which fetches SSURGO data for it
and builds the collection before delegating to the \code{SoilProfileCollection} method).}

\item{...}{Passed to the method.}

\item{seed}{An integer to set the random seed for reproducibility (default \code{123}).}

\item{parallel}{Logical; run the per-profile simulation in parallel via \code{future}/\code{future.apply}
(default \code{FALSE}, matching the pre-0.2.1 default - the parallel path was previously a
separate function, \code{simulate_profile_depths(parallel = TRUE)}).}

\item{n_cores}{Integer, workers to use when \code{parallel = TRUE} (default \code{6}).}

\item{n_simulations}{Integer, the number of triangular draws per component used to derive
\code{sim_comppct} via \code{simulate_component_composition()} (default \code{100}). \code{default} method only.}
}
\value{
A \code{SoilProfileCollection} of simulated/perturbed profiles.
}
\description{
A single entry point for horizon depth/thickness simulation, replacing the pre-0.2.1
\code{simulate_profile_depths()} / \verb{_by_collection()} / \verb{_by_collection_parallel()} trio.
Each profile in the collection is simulated via \code{simulate_and_perturb_soil_profiles()}, then
combined into one \code{SoilProfileCollection}.
}
\section{Note on parallel workers}{

Dispatched via \code{run_parallel_lapply()} (\code{R/parallel.R}), this package's shared
\code{future}/\code{future.apply} helper. \code{future::multisession} workers are fresh R processes;
\code{future}/\code{globals} auto-detect that the per-profile closure calls a \code{soilSIM}-namespaced
function and attach the package in each worker automatically - this only works when \code{soilSIM}
is actually installed and attached in the calling session, not merely \code{devtools::load_all()}'d.
The caller's own \code{future::plan()} (if any) is restored afterward regardless of success/failure.
}

\keyword{internal}
