% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{simulate_profile_depths_by_collection_parallel}
\alias{simulate_profile_depths_by_collection_parallel}
\title{Run Soil Profile Depth Simulations in Parallel for a SoilProfileCollection}
\usage{
simulate_profile_depths_by_collection_parallel(
  soil_collection,
  seed = 123,
  n_cores = 6
)
}
\arguments{
\item{soil_collection}{A SoilProfileCollection object containing soil profile data.}

\item{seed}{An integer value to set the random seed for reproducibility (default is 123).}

\item{n_cores}{Integer, number of cores to use for parallel processing (default is 6).}
}
\value{
A SoilProfileCollection object containing the simulated and perturbed soil profiles.
}
\description{
Simulates soil profile depths for each profile in a \code{SoilProfileCollection}
in parallel using the \code{future}/\code{future.apply} packages, then combines the
results into a single \code{SoilProfileCollection}.
}
\section{Note on parallel workers}{

Dispatched via \code{run_parallel_lapply()} (\code{R/parallel-utils.R}), this package's shared
\code{future}/\code{future.apply} helper. \code{future::multisession} workers are fresh R processes;
\code{future}/\code{globals} auto-detect that \code{simulate_single_profile()} calls a \code{soilSIM}-namespaced
function and attach the package in each worker automatically - this only works when \code{soilSIM}
is actually installed and attached in the calling session, not merely \code{devtools::load_all()}'d.
The caller's own \code{future::plan()} (if any) is restored afterward regardless of success/failure.
}

\examples{
\dontrun{
  simulated_profiles <- simulate_profile_depths_by_collection_parallel(
    soil_collection, seed = 123, n_cores = 6
  )
  print(simulated_profiles)
}

}
