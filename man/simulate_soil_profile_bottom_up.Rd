% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{simulate_soil_profile_bottom_up}
\alias{simulate_soil_profile_bottom_up}
\title{Bottom-up Simulation of Soil Profile}
\usage{
simulate_soil_profile_bottom_up(horizon_data)
}
\arguments{
\item{horizon_data}{A data frame containing horizon data. The data frame must include the following columns:
\itemize{
\item \code{hzname}: The name of the horizon.
\item \code{hzdept_l}: The lower bound for the top depth of the horizon.
\item \code{hzdept_r}: The representative top depth of the horizon.
\item \code{hzdept_h}: The upper bound for the top depth of the horizon.
\item \code{hzdepb_l}: The lower bound for the bottom depth of the horizon.
\item \code{hzdepb_r}: The representative bottom depth of the horizon.
\item \code{hzdepb_h}: The upper bound for the bottom depth of the horizon.
}}
}
\value{
A data frame with the following columns:
\itemize{
\item \code{hzname}: The name of the horizon.
\item \code{top}: The simulated top depth of the horizon.
\item \code{bottom}: The simulated bottom depth of the horizon.
}
}
\description{
Simulates a soil profile from the bottom upward by calculating the top and
bottom depths for each horizon. The simulation starts from the bottom
horizon and proceeds upward, setting the bottom depth of each horizon to
the top depth of the horizon immediately below. For each horizon, the
bottom and top depths are simulated using a triangular distribution via
\code{tri_dist()}. The function ensures that there are no gaps between horizons
and that the top of the uppermost horizon is set to 0.
}
\examples{
horizon_data <- data.frame(
  hzname = c("A", "B", "C"),
  hzdept_l = c(0, 20, 40),
  hzdept_r = c(0, 25, 45),
  hzdept_h = c(0, 30, 50),
  hzdepb_l = c(20, 40, 60),
  hzdepb_r = c(25, 45, 65),
  hzdepb_h = c(30, 50, 70)
)
profile <- simulate_soil_profile_bottom_up(horizon_data)
print(profile)
}
