% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-simulation.R
\name{simulate_soil_profile_thickness}
\alias{simulate_soil_profile_thickness}
\title{Simulate Soil Profile Thickness}
\usage{
simulate_soil_profile_thickness(horizon_data, n_simulations = 500)
}
\arguments{
\item{horizon_data}{A dataframe containing horizon data. It must include the following columns:
- \code{hzname}: The name of each horizon (e.g., A, B, C).
- \code{hzdept_r}: The representative top depth of each horizon.
- \code{hzdepb_r}: The representative bottom depth of each horizon.
- \code{hzdept_l}: The low estimate for the top depth of each horizon.
- \code{hzdept_h}: The high estimate for the top depth of each horizon.
- \code{hzdepb_l}: The low estimate for the bottom depth of each horizon.
- \code{hzdepb_h}: The high estimate for the bottom depth of each horizon.}

\item{n_simulations}{Integer, number of simulations to perform (default is 500).}
}
\value{
A dataframe containing the following columns:
- \code{hzname}: The horizon name.
- \code{top}: The representative top depth of the horizon (from \code{hzdept_r}).
- \code{bottom}: The representative bottom depth of the horizon (from \code{hzdepb_r}).
- \code{thickness_sd}: The standard deviation of the horizon thickness (bottom - top) across the simulations.
}
\description{
Simulates the thickness of soil horizons for a given soil profile using
both top-down and bottom-up simulation methods. It performs multiple
simulations and calculates the standard deviation of horizon thickness
(i.e., the difference between representative bottom and top depths)
across the simulations.
}
\examples{
horizon_data <- data.frame(
  hzname = c("A", "B", "C"),
  hzdept_r = c(0, 20, 35),
  hzdepb_r = c(20, 35, 50),
  hzdept_l = c(0, 15, 30),
  hzdept_h = c(0, 25, 40),
  hzdepb_l = c(15, 30, 45),
  hzdepb_h = c(25, 40, 60)
)
set.seed(123)
summarized_results <- simulate_soil_profile_thickness(horizon_data, n_simulations = 500)
print(summarized_results)

}
