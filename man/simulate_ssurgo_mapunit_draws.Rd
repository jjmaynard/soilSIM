% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo-simulation.R
\docType{data}
\name{simulate_ssurgo_mapunit_draws}
\alias{simulate_ssurgo_mapunit_draws}
\alias{SSURGO_SIM_PROPERTY_COLUMNS}
\title{Monte Carlo-Simulate SSURGO Property Draws for an AOI}
\format{
An object of class \code{character} of length 15.
}
\usage{
simulate_ssurgo_mapunit_draws(
  aoi_vect,
  top_depth,
  bottom_depth,
  n_mc = 1000,
  parallel = FALSE,
  n_cores = NULL,
  config = NULL,
  mukey_raster = NULL,
  depth_windows = NULL,
  requested_properties = NULL,
  seed = NULL
)

SSURGO_SIM_PROPERTY_COLUMNS
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{top_depth, bottom_depth}{Numeric depth window bounds in cm.}

\item{n_mc}{Number of triangular draws \code{sim_component_comp()} uses per component (default 1000).}

\item{parallel, n_cores}{Passed through to \code{maybe_adjust_soil_data_depth_trend()}'s
\code{parallel}/\code{n_cores} - the depth-trend GP adjustment step is this function's dominant cost
for AOIs with many cokeys, and each cokey's GP fitting is independent of every other cokey's.
Default \code{parallel = FALSE} matches prior behavior exactly.}

\item{config}{Optional Monte Carlo config, passed through to
\code{maybe_adjust_soil_data_depth_trend()} -> \code{apply_local_gp_adjustments()} ->
\code{apply_gp_depth_trends()}. \code{config$monte_carlo$vertical_correlation_method} (default
\code{"joint_copula"} default, or \code{"gp_quantile_retrofit"}) selects this top-level entry
point's vertical-correlation method; \code{NULL} (default) resolves to \code{"joint_copula"},
matching \code{get_monte_carlo_defaults()}'s own default.}

\item{mukey_raster}{Optional, already-fetched \code{terra::SpatRaster} of mukey codes for this same
\code{aoi_vect} (e.g. from \code{\link{fetch_ssurgo_mukey_raster}}), passed through to
\code{download_ssurgo_tabular()} so its tabular fetch reuses it instead of issuing a second,
independent \code{soilDB::mukey.wcs()} call. Only consulted on a \code{"ssurgo_tabular"} cache
miss. \code{NULL} (default): the tabular fetch issues its own call.}

\item{depth_windows}{Optional list of \code{c(top, bottom)} numeric pairs. \code{NULL} (default) - one
aggregation over \verb{[top_depth, bottom_depth]}, returning a single data frame exactly as before
(bit-identical). When supplied, the (expensive) per-horizon simulation runs once and is then
aggregated once per window, returning a \strong{named list} of data frames (names \code{"top-bottom"}).
\code{top_depth}/\code{bottom_depth} are ignored in this mode - pass the overall span for them. Built for
\code{extract_mukey_joint_ensemble()}, which needs the SOLUS depth windows from one simulation.}

\item{requested_properties}{\code{NULL} (default) simulates every recognized property, bit-identical
to before. Otherwise a character vector (any vocabulary \code{normalize_requested_properties()}
accepts) restricting \code{simulate_cokey_generalized()} to just those (plus texture coupling) -
the per-cokey GP depth-trend fit then runs once per kept property instead of ~8 times, the
pipeline's dominant saving. A restricted simulation is only \emph{law}-equivalent (not
bit-identical) to slicing the same columns from a full simulation, and only under the default
\code{vertical_correlation_method = "joint_copula"}; under \code{"gp_quantile_retrofit"} (not
subset-invariant - its rank retrofit keys off the first property in the list)
\code{requested_properties} is ignored with a warning and all properties are simulated.}

\item{seed}{Optional integer. \code{NULL} (default) keeps the current stochastic behavior. When set,
\code{set.seed(seed)} runs once at the top (covering the whole sequential RNG stream: component
composition, per-cokey property/texture draws, sequential depth-trend), and the parallel
depth-trend path uses it as \code{future.seed}'s L'Ecuyer seed. Determinism is \strong{conditional} on
identical upstream SSURGO data (\code{download_ssurgo_tabular()} can change over time) and does
\strong{not} make a \code{requested_properties} subset bit-identical to a full simulation.}
}
\value{
With \code{depth_windows = NULL}: a data frame, one row per \code{mukey}/\code{cokey}/\code{simulation_number}
replicate, with simulated property columns aggregated over the depth window - or \code{NULL} if the
tabular fetch fails. With \code{depth_windows} supplied: a named list of such data frames, one per
window (or \code{NULL} on fetch failure). Minor components that \code{download_ssurgo_tabular()}'s
underlying query would otherwise drop entirely (real \code{comppct}, zero \code{chorizon} rows in SDA)
are included transparently here whenever an AOI sibling let them be recovered - see
\code{download_ssurgo_tabular()}'s "Component recovery" section.
}
\description{
Orchestrates the full SSURGO percentile-prior pipeline for one AOI/depth-window: fetch (cached)
tabular SSURGO data, infill missing values, derive \code{genhz}, simulate component composition
(\code{sim_component_comp()}) and join it onto horizons, simulate correlated properties per cokey
(\code{simulate_cokey_generalized()}, using the KSSL reference correlation matrices), optionally
remove organic horizons and apply depth-trend GP adjustment, then aggregate to the requested
depth window.
}
\section{Cache invalidation}{

This function's own disk cache (\code{build_cache_key()}/\code{cache_get()}/\code{cache_set()} via
\code{ssurgo_tabular_cache_key()}) is keyed by \code{aoi_vect} \strong{only} - \code{download_ssurgo_tabular()}
takes no depth-window argument and fetches every horizon for every AOI mukey - and is
separate from \code{download_ssurgo_tabular()}'s own \code{cache_dir} parameter (always \code{NULL} here).
One AOI's tabular download is shared across every depth window/call requested for that AOI.
Clear the cache entry (or the whole cache directory) to force a fresh fetch.

This function's SIMULATED output is not disk-cached: every call re-simulates fresh random
draws. Raw-draws fusion reuses draws in memory within one \code{run_stage1_fusion()} call
(computed once, used for both the percentile cache and \code{mukey_draws_lookup()}); see also
\code{run_stage1_fusion_group()}'s \code{shared_draws} pattern.
}

\keyword{datasets}
