% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo-simulation.R
\docType{data}
\name{simulate_ssurgo_mapunit_draws}
\alias{simulate_ssurgo_mapunit_draws}
\alias{SSURGO_SIM_PROPERTY_COLUMNS}
\title{Monte Carlo-Simulate SSURGO Property Draws for an AOI}
\format{
An object of class \code{character} of length 10.
}
\usage{
simulate_ssurgo_mapunit_draws(
  aoi_vect,
  top_depth,
  bottom_depth,
  n_mc = 1000,
  parallel = FALSE,
  n_cores = NULL,
  config = NULL,
  mukey_raster = NULL
)

SSURGO_SIM_PROPERTY_COLUMNS
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} AOI.}

\item{top_depth, bottom_depth}{Numeric depth window bounds in cm.}

\item{n_mc}{Number of triangular draws \code{sim_component_comp()} uses per component (default 1000).}

\item{parallel, n_cores}{Passed through to \code{maybe_adjust_soil_data_depth_trend()}'s
\code{parallel}/\code{n_cores} - the depth-trend GP adjustment step is this function's dominant cost
for AOIs with many cokeys, and each cokey's GP fitting is independent of every other cokey's.
Default \code{parallel = FALSE} matches prior behavior exactly.}

\item{config}{Optional Monte Carlo config, passed through to
\code{maybe_adjust_soil_data_depth_trend()} -> \code{apply_local_gp_adjustments()} ->
\code{apply_gp_depth_trends()}. \code{config$monte_carlo$vertical_correlation_method} (default
\code{"joint_copula"} as of \code{VERTICAL_CORRELATION_IMPROVEMENT_PLAN.md} Phase 13; set to
\code{"gp_quantile_retrofit"} to opt back into the original algorithm) selects this top-level
entry point's vertical-correlation method; \code{NULL} (default) resolves to \code{"joint_copula"},
matching \code{get_monte_carlo_defaults()}'s own default.}

\item{mukey_raster}{Optional, already-fetched \code{terra::SpatRaster} of mukey codes for this same
\code{aoi_vect} (e.g. from \code{\link{fetch_ssurgo_mukey_raster}}), passed through to
\code{download_ssurgo_tabular()} so its tabular fetch reuses it instead of issuing a second,
independent \code{soilDB::mukey.wcs()} call - see \code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md} task
P1.2/P1.3. Only consulted on a \code{"ssurgo_tabular"} cache miss. \code{NULL} (default) preserves
original behavior exactly.}
}
\value{
A data frame, one row per \code{mukey}/\code{cokey}/\code{simulation_number} replicate, with simulated
property columns aggregated over the depth window - or \code{NULL} if the tabular fetch fails.
Minor components that \code{download_ssurgo_tabular()}'s underlying query would otherwise drop
entirely (real \code{comppct}, zero \code{chorizon} rows in SDA) are included transparently here
whenever an AOI sibling let them be recovered - see \code{download_ssurgo_tabular()}'s "Component
recovery" section.
}
\description{
Orchestrates the full SSURGO percentile-prior pipeline for one AOI/depth-window: fetch (cached)
tabular SSURGO data, infill missing values, derive \code{genhz}, simulate component composition
(\code{sim_component_comp()}) and join it onto horizons, simulate correlated properties per cokey
(\code{simulate_cokey_generalized()}, using the KSSL reference correlation matrices), optionally
remove organic horizons and apply depth-trend GP adjustment, then aggregate to the requested
depth window.
}
\section{Cache invalidation}{

This function's own disk cache (\code{build_cache_key()}/\code{cache_get()}/\code{cache_set()}, keyed by
\code{aoi_vect}/depth window) is separate from \code{download_ssurgo_tabular()}'s own \code{cache_dir}
parameter (always \code{NULL} here). A cache entry for a given AOI/depth-window combination written
before component recovery shipped predates it entirely - clear that cache entry (or the whole
cache directory) to pick up recovered components.

This function's own SIMULATED output (as opposed to the raw tabular SSURGO input it's cached
from) is deliberately NOT disk-cached - every call re-simulates fresh random draws, matching
the original contract exactly. An earlier version of \code{MUKEY_DRAWS_FUSION_IMPROVEMENT_PLAN.md}
task P2.1 added an unconditional disk cache here so raster-fusion's raw-draws opt-in
(\code{R/raster-fusion.R}'s \code{fuse_general_kde()}) could reuse draws across calls - reverted after
review: it silently froze repeat output for EVERY caller of this function (including ones with
nothing to do with raw-draws fusion, e.g. direct \code{fetch_ssurgo_percentiles()} use), which broke
this project's own "default unchanged until opted in" convention. Raw-draws fusion instead
reuses draws purely in-memory within one \code{run_stage1_fusion()} call (compute once, use for both
the percentile cache and \code{mukey_draws_lookup()}) - see that function's implementation and
\code{run_stage1_fusion_group()}'s pre-existing \code{shared_draws} pattern, which already did this.
}

\keyword{datasets}
