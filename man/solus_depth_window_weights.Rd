% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solus-simulation.R
\name{solus_depth_window_weights}
\alias{solus_depth_window_weights}
\title{Trapezoidal Depth-Average Weights over Native SOLUS Depth Slices}
\usage{
solus_depth_window_weights(
  top_depth,
  bottom_depth,
  available_slices = c(0, 5, 15, 30, 60, 100, 150)
)
}
\arguments{
\item{top_depth, bottom_depth}{Numeric depth window bounds in cm (\code{bottom_depth > top_depth}).}

\item{available_slices}{Native SOLUS depth points (default \code{c(0, 5, 15, 30, 60, 100, 150)}).}
}
\value{
A named numeric vector over \code{sort(unique(available_slices))} (names are the slice depths
as character), summing to 1. Zero for slices that do not contribute.
}
\description{
Returns the weights that turn SOLUS100's point predictions at the native depth slices into an
estimate of the \strong{depth-average} of the property over \verb{[top_depth, bottom_depth]} -
\code{sum(weights * slice_values)} approximates \verb{(1 / (b - a)) * integral_a^b f(d) dd}, where \code{f} is
taken piecewise-linear between native slices (with \code{rule = 2} constant extrapolation outside the
\verb{[0, 150]} span SOLUS predicts over). This is the raster analogue of the thickness-weighted
window mean the SSURGO prior side already computes (\code{aggregate_depth_window_by_replicate()}), so
the two sides of \code{fuse_property_adaptive()} represent the same estimand.
}
\details{
Exact for a linear depth profile (reduces to the midpoint value, matching
\code{closest_solus_depth_slice()}); unbiased to second order for curved profiles (OC decay, clay
bulge, pH trend). Cost is a single weighted raster-stack sum - \code{length(available_slices)} is
always small, so this scales with the number of slices, not the number of pixels.

Windows lying entirely at/beyond the deepest slice (or above the shallowest) fall back to the
single nearest slice (constant extrapolation).
}
\seealso{
\code{closest_solus_depth_slice()}, \code{fetch_solus_low_pred_high()}
}
