% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cache.R
\name{ssurgo_tabular_cache_key}
\alias{ssurgo_tabular_cache_key}
\title{Build a cache key for one AOI's raw SSURGO tabular download (depth-independent)}
\usage{
ssurgo_tabular_cache_key(aoi_vect)
}
\arguments{
\item{aoi_vect}{A \code{terra::SpatVector} (single-feature AOI).}
}
\value{
A character string, safe for use as a filename.
}
\description{
\code{download_ssurgo_tabular()} (\code{R/ssurgo-acquisition.R}) takes no depth-window argument at all -
it fetches every horizon for every AOI mukey; depth filtering happens later, per call, in
\code{aggregate_depth_window_by_replicate()}. The downloaded tabular data therefore depends only on
the AOI (and the fixed default \code{properties} list every call site uses - never varied), not on
any depth window. Keying the tabular cache on \code{top_depth}/\code{bottom_depth} (as it was before this
function existed -) fragments one AOI's cache into one
unusable copy per distinct window ever requested for it, so e.g. \code{run_stage1_fusion_multi()}'s
wide-span simulation and a later single-window \code{run_stage1_fusion()} call for the same AOI never
shared a tabular download. Mirrors \code{mukey_grid_cache_key()}'s fixed-sentinel pattern.
}
\keyword{internal}
