% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{stage1_fuse_texture_group_from_fetched}
\alias{stage1_fuse_texture_group_from_fetched}
\title{Fuse an Already-Fetched Texture Group (Stage 1 group-fusion tail)}
\usage{
stage1_fuse_texture_group_from_fetched(
  fetched,
  want_raw_draws = FALSE,
  shared_draws = NULL,
  shared_mukey_raster = NULL,
  reference_grid = fetched[[1]]$lik[[1]]
)
}
\arguments{
\item{fetched}{A list (one entry per group member, in \code{group_members()} order) of
\verb{list(id=, prior=<aligned percentile-value rasters>, prior_probs=, lik=<value rasters>, lik_probs=)}.}

\item{want_raw_draws}{Whether any member requested \code{prior_fusion_method = "raw_draws"} - the
raw-draws texture path only runs when this is \code{TRUE} (a cold percentile cache alone can leave
\code{shared_draws} non-\code{NULL} without raw-draws fusion being requested).}

\item{shared_draws, shared_mukey_raster}{The one-per-group in-memory draws data frame and its
native-grid mukey raster (already computed by the caller); both required for the raw-draws
path.}

\item{reference_grid}{The grid every \code{fetched} member's prior/likelihood was already aligned
onto (i.e. \code{align_prior_likelihood()}'s \code{reference_grid} from aligning the first member) -
the mukey raster is aligned onto this same grid, regardless of which side (prior or
likelihood) it came from. Defaults to \code{fetched[[1]]$lik[[1]]} for backward compatibility
with callers that haven't been updated to pass it explicitly (matches the original
\code{resampling = "down"}-only behavior).}
}
\value{
\code{fuse_texture_group()}'s return value - a named list keyed by member id.
}
\description{
The portion of \code{\link[=run_fusion_group]{run_fusion_group()}} that runs once every member's aligned SSURGO prior
and SOLUS likelihood are assembled into \code{fetched}: optionally prepare the real joint per-mukey
texture draws for raw-draws fusion, then fuse the group via \code{\link[=fuse_texture_group]{fuse_texture_group()}}. Factored out
so \code{run_fusion_group()} and \code{run_fusion_multiproperty()} share one group-fusion code path.
}
\keyword{internal}
