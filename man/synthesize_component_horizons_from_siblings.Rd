% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo-acquisition.R
\name{synthesize_component_horizons_from_siblings}
\alias{synthesize_component_horizons_from_siblings}
\title{Synthesize a Representative Horizon Profile for a Component from AOI Siblings}
\usage{
synthesize_component_horizons_from_siblings(target_component, sibling_horizons)
}
\arguments{
\item{target_component}{One-row data frame: \code{mukey}, \code{cokey}, \code{compname}, \code{comppct_l},
\code{comppct_r}, \code{comppct_h}.}

\item{sibling_horizons}{Data frame of horizon rows (same column shape as
\code{download_ssurgo_tabular()}'s \code{ssurgo_data}) from one or more OTHER cokeys sharing
\code{target_component$compname}.}
}
\value{
Data frame of synthesized horizon rows for \code{target_component$cokey}, one row per genhz
group produced, with \code{infill_method = "component_aoi_average"} and
\code{component_synthesized = TRUE} on every row. A 0-row data frame if no sibling horizon was
classifiable at all.
}
\description{
Given one component known to exist (via a real \code{comppct}) but missing all horizon data
(\code{target_component}), and the already fully processed horizon rows of OTHER cokeys within the
same \code{download_ssurgo_tabular()} call's \code{ssurgo_data} that share \code{target_component$compname}
(\code{sibling_horizons}), builds a synthetic horizon profile for \code{target_component$cokey} by
averaging sibling horizons within each \code{classify_genhz()} group.
}
\section{Alignment method}{

Horizons are aligned by \code{classify_genhz(hzname)} group (\code{O}/\code{A}/\code{E}/\code{B}/\code{C}/\code{Cr}/\code{R}), not by
position or depth bin - robust to sibling profiles having different horizon counts or depths.
Sibling rows whose \code{hzname} doesn't classify (\code{classify_genhz()} returns \code{NA}) are dropped from
averaging entirely rather than guessed at - verified before choosing this: no depth-or-property-
based genhz fallback exists anywhere else in soilSIM to reuse (\code{classify_genhz()} itself and the
separate \code{aqp::generalizeHz()} usage in \code{R/depth-simulation.R} are both purely \code{hzname}-regex
matchers).
}

\section{Averaging rule per genhz group}{

\itemize{
\item Numeric columns (depths, thicknesses, every property column present): arithmetic mean
across whichever sibling rows fall in that genhz group, \code{na.rm = TRUE}.
\item \code{hzname}: the most frequent \code{hzname} string among the group's sibling rows (ties broken
alphabetically) - reused verbatim, never invented, so it re-classifies to the same genhz
group deterministically.
\item Other character/logical columns (\code{texcl}, \code{desgnmaster}, \code{unsuitable_horizon}, etc.):
most frequent non-\code{NA} value in the group; \code{NA} if every sibling value in the group is \code{NA}.
\item \code{mukey}/\code{cokey}/\code{compname}/\code{comppct_l}/\code{comppct_r}/\code{comppct_h}: taken from
\code{target_component}, never averaged from siblings - the missing component's own real identity
and composition percentages are already known and correct.
\item \code{chkey}: freshly generated (\code{"synth_<cokey>_<genhz>"}), unique and clearly synthetic.
}
A genhz group present in only one sibling still produces a row (mean of one = a copy); a group
present in zero siblings is simply absent from the output - never fabricated from nothing.
}

\keyword{internal}
