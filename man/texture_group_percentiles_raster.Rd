% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-fusion.R
\name{texture_group_percentiles_raster}
\alias{texture_group_percentiles_raster}
\title{Raster-native ILR-posterior percentile sampler for \code{fuse_texture_group()}}
\usage{
texture_group_percentiles_raster(
  ilr_mu_r,
  ilr_Sigma_r,
  posterior_probs,
  n_mc = 2000,
  max_cells_per_subchunk = 2000
)
}
\arguments{
\item{ilr_mu_r}{2-layer SpatRaster (\code{mu1}, \code{mu2}) - \code{fuse_texture_group_batch()}'s \code{ilr_mu} output.}

\item{ilr_Sigma_r}{3-layer SpatRaster (\code{S11}, \code{S12}, \code{S22}) - that function's \code{ilr_Sigma} output.}

\item{posterior_probs}{Numeric probabilities (0-1) at which to report posterior percentiles.}

\item{n_mc}{Monte Carlo draw count per cell. Default 2000 matches this file's existing
\code{texture_n_mc} convention (the separate MC sample size \code{fuse_texture_group_batch_core()} uses
to ESTIMATE the \verb{(mu1,mu2,S11,S12,S22)} moments in the first place) - a coincidentally shared
default, not the same computation; this parameter controls a distinct, later sampling step
that draws FROM the already-fused posterior distribution.}

\item{max_cells_per_subchunk}{Same memory rationale as \code{fuse_texture_group_batch()}'s own
parameter - each cell needs \code{2*n_mc} standard-normal draws plus a \code{3*n_mc}-row ILR-inverse
composition matrix, so large \code{terra::app()} chunks can exhaust memory.}
}
\value{
\code{list(clay=, sand=, silt=)}, each a named list of percentile \code{SpatRaster}s (names
\code{"P1"}, \code{"P50"}, ... matching \code{posterior_probs}).
}
\description{
Draws \code{n_mc} bivariate-Normal ILR-space samples per cell from the fused \verb{(mu1, mu2, S11, S12, S22)} posterior - the same distribution \code{R/distributions.R}'s \code{sample_ilr_posterior()} draws
from in the scalar case, here vectorized raster-wide via \code{terra::app()} - applies each cell's
own 2x2 Cholesky factor (closed-form: \code{u11 = sqrt(S11)}, \code{u12 = S12/u11}, \code{u22 = sqrt(S22 - u12^2)}, matching base R's \code{chol()} upper-triangular convention exactly), \code{ilr_inverse()}s the
result to \verb{(clay, sand, silt)} triples, then computes \code{quantile()} across draws per cell per
fraction. Unlike \code{bayesian_update()}'s grid-based routes, this posterior has no discretized-grid
shortcut - it's a genuine bivariate Monte Carlo sampler, so \code{n_mc} is the only lever on
percentile reliability here.
}
\section{Sum-to-100 caveat (expected, not a bug)}{

The point estimate (\code{posterior$value}, via \code{ilr_inverse()} on the fused mean) sums to 100 by
construction for every cell. Per-fraction PERCENTILES do \strong{not} generally sum to 100 -
percentiles of correlated marginals don't add linearly (e.g. clay's P95 and sand's P95 can both
be simultaneously "high" draws from different tail regions of the joint distribution, so their
sum can exceed - or their P5s' sum can fall short of - 100). This is an expected mathematical
property of taking marginal percentiles of a compositional joint distribution, not a defect.
}

\keyword{internal}
