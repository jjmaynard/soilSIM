% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{tri_dist}
\alias{tri_dist}
\title{Draw random samples from a triangular distribution}
\usage{
tri_dist(n = 1, a = 0, b = 1, c = (a + b)/2)
}
\arguments{
\item{n}{Number of samples to draw. If a vector is passed, its length is
used (matching \code{stats::rnorm()}-style conventions).}

\item{a, b, c}{Minimum, maximum, and mode of the triangular distribution.}
}
\value{
A numeric vector of length \code{n}; \code{NaN} for every element if \code{a},
\code{b}, \code{c} describe a degenerate/invalid triangle.
}
\description{
Ported verbatim from \code{code_ref/brdf/property_simulation.R}'s \code{tri_dist()}
(originally adapted from the \code{triangle} package), used by
\code{R/depth-simulation.R}'s profile-depth simulators. Kept as its own
random-draw implementation rather than layered on \code{quantile_triangular()}
(a deterministic inverse-CDF evaluator with different degenerate-input
handling) so its exact edge-case behavior is preserved: it errors on
invalid \code{n}, but returns \code{NaN} (not an error) when the mode falls outside
\verb{[a, b]} or any parameter is infinite/\code{NA}.
}
