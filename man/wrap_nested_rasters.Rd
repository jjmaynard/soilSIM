% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cache.R
\name{wrap_nested_rasters}
\alias{wrap_nested_rasters}
\alias{unwrap_nested_rasters}
\title{Wrap/unwrap every \code{SpatRaster} found anywhere inside an arbitrarily-nested list}
\usage{
wrap_nested_rasters(x)

unwrap_nested_rasters(x)
}
\arguments{
\item{x}{A list, arbitrarily nested, that may contain \code{SpatRaster} objects at any depth.}
}
\value{
\code{x}, with every \code{SpatRaster} replaced by its wrapped/unwrapped equivalent.
}
\description{
A generic counterpart to \code{wrap_percentile_list()}/\code{unwrap_percentile_list()}, for result
structures whose shape isn't the fixed \code{list(values=, probs=)} percentile form - e.g.
\code{run_stage1_fusion()}'s return value, which nests \code{SpatRaster}s at \code{prior$values},
\code{likelihood$values}, and inside \code{posterior} (whose own shape varies by \code{dist}). Recurses into
every list element, replacing each \code{SpatRaster} with \code{terra::wrap()}'s \code{PackedSpatRaster}
representation (or the reverse via \code{terra::unwrap()}) and leaving everything else untouched -
directly addresses \code{cache_set()}'s own \verb{@section Known limitation:} for callers caching a
full fusion result rather than a raw percentile fetch.
}
