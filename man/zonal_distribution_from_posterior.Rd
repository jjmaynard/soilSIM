% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fusion.R
\name{zonal_distribution_from_posterior}
\alias{zonal_distribution_from_posterior}
\title{Zonal (per-mukey) Reduction of a Fused Posterior Raster - Benchmark Baseline}
\usage{
zonal_distribution_from_posterior(
  posterior,
  mukey_raster,
  probs = c(0.05, 0.5, 0.95)
)
}
\arguments{
\item{posterior}{A \code{run_fusion()}-style \code{list(percentiles = , ...)}.}

\item{mukey_raster}{A categorical mukey \code{terra::SpatRaster} (any grid - resampled to the
posterior grid via nearest-neighbour internally).}

\item{probs}{Length-3 ascending probabilities for the \code{low}/\code{rep}/\code{high} triplet; each must be
one of the posterior's own percentile-layer probabilities (no interpolation).}
}
\value{
Named list keyed by mukey code, each element \code{c(low =, rep =, high =)}.
}
\description{
Collapses \code{run_fusion()}'s per-pixel posterior to one distribution per mukey by taking
\code{terra::zonal()} means of its percentile layers, in the \code{low}/\code{rep}/\code{high} shape
\code{fuse_observed_data_into_priors()}'s \code{observed_data_by_mukey} accepts. \strong{Percentile-based only -
there is deliberately no point-estimate ("mean") path}, which would narrow the fed-back prior
and understate uncertainty (see the design doc's Problem A redraft).
}
\details{
Its sole purpose is the A.6 benchmark: the mukey-collapsed comparison arm for
\code{remarginalize_ensemble_to_posterior()}, run through the \emph{same} tabular Monte Carlo machinery
via \code{observed_data_by_mukey}. Kept \verb{@keywords internal} until that benchmark says whether the
cheap path is adequate for production (then promote) or not (then delete).
}
\keyword{internal}
