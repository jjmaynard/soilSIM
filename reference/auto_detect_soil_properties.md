# Auto-detect Soil Properties

Automatically detects available soil properties in a dataset.

## Usage

``` r
auto_detect_soil_properties(df)
```

## Arguments

- df:

  Input data frame

## Value

Vector of detected property names
