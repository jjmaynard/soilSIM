# Compute the Mode of a Numeric Vector

Compute the Mode of a Numeric Vector

## Usage

``` r
calculate_mode(x)
```

## Arguments

- x:

  A vector of values.

## Value

The most frequently-occurring value in `x`.
