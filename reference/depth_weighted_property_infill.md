# Depth Weighted Property Infill

Depth Weighted Property Infill

## Usage

``` r
depth_weighted_property_infill(group, property_col, problematic_mask)
```

## Arguments

- group:

  Data frame group

- property_col:

  Property column name

- problematic_mask:

  Logical mask for problematic horizons

## Value

Data frame with infilled values
