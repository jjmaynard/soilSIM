# Escape Text for Inclusion in HTML

Escape Text for Inclusion in HTML

## Usage

``` r
escape_html_text(x)
```

## Arguments

- x:

  Character vector.

## Value

Character vector with `&`, `<`, `>` escaped.
