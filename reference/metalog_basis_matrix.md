# Metalog basis matrix

Column `j` is the j-th metalog basis function evaluated at each
probability in `y`.

## Usage

``` r
metalog_basis_matrix(y, term)
```

## Arguments

- y:

  Numeric vector of probabilities (0,1).

- term:

  Number of metalog terms.
