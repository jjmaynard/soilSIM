# Diagnostic report

Wraps the list a `diagnose_*()` / master `validate_*()` reporter
returns.

## Usage

``` r
new_soilSIM_diagnostics(x)
```

## Arguments

- x:

  A diagnostic-report list.

## Value

An object of class `soilSIM_diagnostics`.
