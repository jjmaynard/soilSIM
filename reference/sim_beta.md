# Beta distribution sampler (fits a Beta distribution to percentiles rescaled to the unit interval)

Beta distribution sampler (fits a Beta distribution to percentiles
rescaled to the unit interval)

## Usage

``` r
sim_beta(probs, values, n, bounds)
```

## Arguments

- bounds:

  Length-2 vector of (lower, upper) support bounds. Required.
