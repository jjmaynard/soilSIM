# Piecewise-linear inverse-CDF sampler (nonparametric, exact at knots, clamped tails)

Piecewise-linear inverse-CDF sampler (nonparametric, exact at knots,
clamped tails)

## Usage

``` r
sim_linear_cdf(probs, values, n)
```
