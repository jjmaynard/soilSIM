# Normal distribution sampler (P50 as mean; SD from the widest available percentile pair)

Normal distribution sampler (P50 as mean; SD from the widest available
percentile pair)

## Usage

``` r
sim_normal(probs, values, n)
```
