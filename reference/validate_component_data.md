# Validate Component Composition Data

Validate Component Composition Data

## Usage

``` r
validate_component_data(component_data)
```

## Arguments

- component_data:

  Data frame expected to have a `comppct_r` column.

## Value

List with `valid`, `message`.
